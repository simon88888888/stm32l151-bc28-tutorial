# STM32L151 + BC28 开发板 · 操作手册（第 2 篇：BC28 联网与 AT 指令实测）

> **系列目录 → [`README.md`](README.md)**

> ### 这是系列的第 2 篇
>
> **[第 1 篇](第1篇-搭好初始环境.md)** 干的是「编译 → ST-Link 下载 → 串口打印」跑通。
> 本篇接着往下：**让 BC28 联上网，并且把 AT 指令一条条敲实。**
>
> | 篇 | 主题 | 状态 |
> |---|---|---|
> | [第 1 篇](第1篇-搭好初始环境.md) | 搭好初始环境（编译 / 下载 / 看日志） | ✅ 已发布 |
> | **第 2 篇** | **BC28 联网 + AT 指令实测** | ✅ **本篇** |
> | 第 3 篇 | 把数据发到自己的服务器（固件里写，不用 PC） | ⬜ 待写 |
> | 第 4 篇 | 墨水屏（这块板子出厂配的那个屏） | ⬜ 待写 |
>
> **本篇的写法**：下面每一条 AT 指令**都是在本板上真的敲过、并且成功了**，回显是照抄的。
> 凡是**没成功 / 没定论**的，一律单独列在 **§8**，不混进正文冒充"能用"。
>
> **实测环境**：2026-09-12，Windows 11 + Keil MDK 5.24a + ST-Link V2 克隆版，
> 中国移动 NB-IoT 卡，`<COM 口>` = COM7，模块固件 `BC28CNVBAR02A03`。

**一句话结论**：这块板子**能上网、能收发 TCP**。但**"敲 AT"这条路是本篇自己搭出来的** ——
板子上三个 USB 口**没有一个能直连模块**（§2），必须先烧一份**透传固件**把 MCU 变成一根"会搬字节的线"。

---

## 0. 三步速查（熟了只看这一节）

```
① 烧透传固件：04-AT透传/main.c（建法见 §2.3 + 04-AT透传/README.md）
② 插 USB1，双击 flash_and_run.bat，确认 flash.log 里有 Application running ...
③ cd <工具目录> && python at_console.py "ATI" "AT+CSQ" "AT+CEREG?"
     —— 三个都有回显 = 通了
```

---

## 1. 先确认你有没有卡和信号（一分钟）

在敲任何联网指令**之前**先把这一步做掉，否则"代码不对"和"没信号"会混在一起。

| 敲什么 | 好消息长这样 | 坏消息 |
|---|---|---|
| `AT+CSQ` | `+CSQ:28,99`（第一个数 >10 就行） | `+CSQ:99,99` = 没信号，先查天线 |
| `AT+CEREG?` | `+CEREG:0,1`（**末尾是 1 = 已注网**）或 `0,5`（已注网+漫游） | `0,2` = 正在搜网；`0,0` = 没在搜 |
| `AT+CGATT?` | `+CGATT:1`（附着上了） | `+CGATT:0` |

**三个都对上，才继续往下。** 这一步不过，后面所有 socket 指令都会失败，而且失败信息很难看。

> ⚠️ **BC28 只支持 NB-IoT**，没有 2G/4G 兜底。NB-IoT 这两年国内在减频，
> **部分地区/运营商已经收不到**。这一条是第 1 篇 §9.3 强调过的，
> 到本篇就成了实打实的第一道门。**天线必须接**（板上是 IPEX 座；不接天线很可能 `CSQ:99,99`，
> 别拿没接天线的状态判断"模块坏了"）。

---

## 2. 硬约束：PC 没有直连 BC28 的路（这一节是本篇的地基）

### 2.1 三个 USB 口，一个都到不了模块的 MAIN 口

| 板上的 USB 口 | 后面接的是 | 能不能敲 AT |
|---|---|---|
| **USB1** | CH340G → STM32 USART1 | ❌ 只能到 MCU，到不了模块 |
| USB2 | FT232(U12) → SW8 拨码 → 模块的 **DBG/AUX** | ❌ 它不是 MAIN 口 |
| 核心板的 MINI_USB | 直连 BC28 的 USB_DP/DM | ❌ 只能升级固件（QFlash） |

BC28 的 **MAIN 串口**（模块 17/18 脚）**只连到 STM32 的 USART2**
（PA2=TX / PA3=RX，经 TXS0108E 电平转换 + R58/R59 0R）。

> **这就是那个"最常见的坑"**：插上 USB1、打开串口助手、敲 `AT`、一个字都不回，
> 于是判断"模块坏了 / 卡没插好 / 天线不灵"。**其实那句 `AT` 根本没走到模块跟前。**

### 2.2 所以：让 MCU 当一根"会搬字节的线"

```
PC (at_console.py / 串口助手)
      │  9600 8-N-1, 无流控
   USB1 │ CH340G
      ▼
 STM32 USART1  ──►  main() 主循环  ──►  USART2  ──►  BC28 MAIN
      (轮询)          (双向搬字节)        (轮询)
```

这份固件就是 **[`04-AT透传/main.c`](04-AT透传/main.c)**（我的代码，
和厂家模板的 diff 在同目录 `main.c.diff`，83 删 / 59 增）。
它**不含任何应用逻辑** —— 跑起来串口是"空"的，等你在 PC 上敲。

> 它**不是**厂家例程里那种应用固件。那种一上电自己就 `AT+CGATT=1`、建 socket 发数据，
> 串口全被它自己占了，你敲什么都是"撞车"。

### 2.3 怎么把它建起来 + 烧进去

1. **建**：拿资料包 **例程 1**（ST 的 `STM32L1xx_StdPeriph_Templates`）拷到短路径 →
   跑 **`02-工具/prep_project.py`**（修那三处缺陷，第 1 篇 §5.2）→
   用 **`04-AT透传/main.c`** 覆盖 `Project\Test\main.c` → 编译。
   *（可选精简：去掉 `.uvproj` target 1 里的 `HARDWARE/{BC28,eink,TIMER}` 和 `Utilities\`，
   67 MB → 4 MB、4 个 target → 1 个。见 `04-AT透传/README.md`）*
2. **烧**：把 `02-工具/flash_and_run.bat` 拷到 `Project\Test\MDK-ARM\` 下双击。
   **判据：`flash.log` 里有 `Application running ...`**。
3. **验证在跑**：串口先打一段 banner，之后静默：
   ```
   ### BC28 AT bridge ###
   build: Sep 12 2026 23:13:29
   PC port: 9600 8-N-1, flow control = none. Type AT commands.
   ```
   串口没动静时，看 **PC3 那颗灯有没有在慢闪**（空闲心跳）—— 闪 = 固件在跑。

### 2.4 换掉透传固件之后，串口助手要改的只有一个地方

**9600 8-N-1，流控必须"无"。** 其它都是默认值。流控若被打开（很多人上一次开过就忘了），
表现是"能发不能收"或"收到乱码"。

---

## 3. 怎么敲：`at_console.py`

工具在 **[`02-工具/at_console.py`](02-工具/at_console.py)**（自动找 CH340 那个 COM 口）：

```bash
cd <工具目录>
python at_console.py "ATI" "AT+CSQ" "AT+CEREG?"      # 默认每条等 2 秒
python at_console.py -w 5 "AT+NUESTATS"              # 回显慢的，等 5 秒
python at_console.py -p COM7 "AT"                    # 手动指定端口
```

每条前面打 `[sent] ...`，模块的回显按行打 `  | ...`，原样不加工。

> ⚠️ **PowerShell 里带引号的 AT 指令，外层要用单引号**：
> `python at_console.py -w 12 'AT+NPING="1.2.3.4"'`
> 用双引号包外层，内层引号会被 PowerShell 剥掉，命令被切碎成两个参数发出去。
> （在 CMD / Git Bash 里没这个问题。）

---

## 4. AT 指令实测表（本篇主体）

**下面每一条都真敲过。** 回显里凡涉及**卡/模块的身份信息**（IMEI、IMSI、ICCID、
模块内网 IP、Cell ID）**一律打了码** —— 这些是能定位到具体卡片和位置的数据，
发到公开仓库前先遮掉。**你自己跑出来的会是完整的数字，那才是对的。**

### 4.1 基础

| 指令 | 回显 | 说明 |
|---|---|---|
| `AT` | `OK` | 活着。**不通先查这个** |
| `ATI` | `Quectel`<br>`BC28-CNV`<br>`Revision:BC28CNVBAR02A03`<br>`OK` | 厂家 / 型号 / 固件版本 |
| `AT+CGMI` | `Quectel` | 厂商 |
| `AT+CGMM` | `Quectel_BC28-CNV` | 型号 |
| `AT+CGMR` | 4 段，每段一行版本号，末尾都带 `V100R001C00SPC051`：<br>`SEBoot` / `PROTOCOL` / `APPLICATION` / `SEMain`<br>外加 `RADIO,RF_010117_202209090109` | **最全的版本信息**。刷固件前后对比它 |

### 4.2 卡的号码（**回显已打码**）

| 指令 | 回显（打码后） | 是什么 |
|---|---|---|
| `AT+CIMI` | `46008**********` | **IMSI**。头 5 位 `46008` = 中国移动的物联网号段 |
| `AT+CGSN=1` | `+CGSN:86429405*******` | **IMEI**。`=1` 才回 IMEI |
| `AT+CGSN` | `MPD22I8*******` | ⚠️ **不带参数回的不是 IMEI**，是模块的产品序列号（一串带字母的） |
| `AT+NCCID` | `+NCCID:898604b5102*********` | **ICCID**。`898604` = 中国移动 NB-IoT 卡 |

> `AT+CGSN=1` 和 `AT+CGSN` 回的东西完全不一样，**别把后者当 IMEI 用**。
> IMEI 是**模块**的，IMSI/ICCID 是**卡**的 —— 换卡 IMEI 不变。

### 4.3 网络注册状态

| 指令 | 实测回显 | 怎么看 |
|---|---|---|
| `AT+CSQ` | `+CSQ:28,99` | 第一个数 = 信号，**换算 `RSSI = -113 + 2×n` dBm** → `28` = **−57 dBm**（很不错）。第二个数 99 = 未提供 |
| `AT+CEREG?` | `+CEREG:0,1` | **末尾 1 = 已注册**（`5` = 已注册+漫游）。`2`=搜网中，`0`=没搜 |
| `AT+CGATT?` | `+CGATT:1` | 1 = PS 域已附着，**可以建 PDP 了** |
| `AT+CSCON?` | `+CSCON:0,0` | 信令连接状态。`0` = 空闲态（NB-IoT 常见，正常，**不代表掉线**） |
| `AT+COPS?` | `+COPS:0,2,"46000"` | `2` = 已自动选了网；`"46000"` = 中国移动 |
| `AT+NBAND?` | `+NBAND:5,8,3` | 支持 Band 5 / 8 / 3，**顺序就是优先级** |
| `AT+CFUN?` | `+CFUN:1` | 1 = 全功能模式 |
| `AT+CEER` | `OK` | 查上一个错误原因。回 `OK`（不带原因）= 没有待报的错误 |

### 4.4 信号质量：`AT+NUESTATS`（**注意单位是 0.1**）

```
AT+NUESTATS
+NUESTATS: Signal power:-683
+NUESTATS: Total power:-570
+NUESTATS: TX power:83
+NUESTATS: TX time:3260
+NUESTATS: RX time:675349
+NUESTATS: Cell ID:1923****
+NUESTATS: ECL:0
+NUESTATS: SNR:64
+NUESTATS: EARFCN:3685
+NUESTATS: PCI:**
+NUESTATS: RSRQ:-106
+NUESTATS: OPERATOR MODE:4
+NUESTATS: CURRENT BAND:8
OK
```

**⚠️ 这里所有的 dB 类数值都以 0.1 为单位，读之前先除 10：**

| 字段 | 原始值 | 实际值 | 是什么 |
|---|---|---|---|
| `Signal power` | `-683` | **−68.3 dBm** | RSRP（有用信号） |
| `Total power` | `-570` | **−57.0 dBm** | RSSI（含干扰） |
| `SNR` | `64` | **6.4 dB** | 信噪比。>3 能用，>10 挺好 |
| `RSRQ` | `-106` | **−10.6 dB** | 参考信号质量 |
| `TX power` | `83` | **8.3 dBm** | 发射功率 |
| `ECL` | `0` | 0 | 覆盖等级，**0 是最好的那档** |
| `EARFCN` | `3685` | — | 频点。Band 8 的范围是 3450–3799，**和 `CURRENT BAND:8` 对得上** |
| `PCI` | *（打码）* | — | 基站小区物理 ID。**和 Cell ID 合起来能定位到具体小区**，所以一起遮掉 |
| `Cell ID` | *（打码）* | — | 服务小区号 —— 能定位到你所在的小区，**发出去前遮掉** |
| `OPERATOR MODE` | `4` | — | 4 = 自动选网 |

> **一次交叉验证**：`AT+CSQ` 给 `28` → 换算 −57 dBm，而 `NUESTATS` 的
> `Total power` 正好是 `-570` → −57.0 dBm。**两边对上了**，说明"除以 10"这个读法是对的，
> 也说明这两条指令量的是同一个东西（RSSI）。

> `AT+NUESTATS` 的**读数是会变的**（小区、PCI 都可能变）。我两次跑，
> Cell ID / PCI 就不一样 —— 见 §8。**别把它当成固定值去写死在代码里。**

### 4.5 PDP 上下文与 APN（**这里藏着厂家代码的一个坑**）

| 指令 | 实测回显 |
|---|---|
| `AT+CGACT?` | `+CGACT:0,1`<br>`+CGACT:1,0` |
| `AT+CGDCONT?` | `+CGDCONT:0,"IPV4V6","","",0,0,0,0,0,0`<br>`+CGDCONT:1,"IP","HUAWEI.COM","",0,0,0,0,0,0` |
| `AT+CGCONTRDP` | `+CGCONTRDP:0,,"CMNBIOT.MNC008.MCC460.GPRS"` |
| `AT+CGPADDR` | `+CGPADDR:0,10.120.x.x`（打码）<br>`+CGPADDR:1` |
| `AT+CGDCONT=?` | `+CGDCONT:(0-10),("IP","IPV6","IPV4V6","NONIP"),,,,(0,1),,,,,(0,1),(0,1)` |

**读法**：

- `AT+CGACT?` 列出每个 cid **有没有被激活**：**cid 0 是激活的（末尾 1），cid 1 没激活（0）**。
- `AT+CGCONTRDP` 告诉你**当前真正在用的那个 APN** —— 是 **`CMNBIOT.MNC008.MCC460.GPRS`**，
  这是**中国移动 NB-IoT 的网络侧配置**。
- 而 `AT+CGDCONT?` 里 **cid 1 写的是 `HUAWEI.COM`** —— **这是厂家留下的一条没用的配置**，
  它从来没被激活过（`CGACT` 里 cid 1 = 0）。

> **这就是"照厂家代码改了 APN 却毫无反应"的原因**：真正生效的是 cid 0，
> 而它的 APN 是网络下发的，你在 cid 1 里怎么改都不影响它。
>
> **实践建议：这张卡不要自己写 APN。** 移动的 NB-IoT 卡走网络下发（`CGDCONT` 里的 cid 0
> 保持空、`CGACT` 去激活 cid 0 即可），自己填的 APN 反而可能把它顶掉。
> 换卡（电信/联通）之后再按运营商给的 APN 填 —— **那一步本篇没验证过**。

- `AT+CGPADDR` 给出模块**拿到的 IP**（`+CGPADDR:1` 后面空 = cid 1 没有 IP，印证了它没激活）。
  这个 IP 是运营商大内网地址，**对外没有意义**，发出去前也遮一下。

### 4.6 省电模式：PSM / eDRX（**出厂就是开着的**）

| 指令 | 实测回显 | 说明 |
|---|---|---|
| `AT+CPSMS?` | `+CPSMS:1,,,01000001,00000101` | **`1` = PSM 开着**。后两段是 TAU / Active Time 的编码 |
| `AT+CPSMS=0` | `OK` | **关掉 PSM**（实测可关） |
| `AT+CEDRXS?` | `+CEDRXS:5,"0010"` | eDRX 状态，`5` = 同时允许 3GPP 和 3GPP2 的 eDRX |
| `AT+NPTWEDRXS?` | `+NPTWEDRXS:5,"0000","0010"` | 更细的 eDRX 参数 |

> **为什么要关心**：这两个开着，模块会**周期性睡下去**，收发数据的延迟可以是**十几秒级**。
> 本篇 §5 那两个时序坑，都跟它有关系。
> **做交互式调试时，先把 PSM 关掉（`AT+CPSMS=0`）会舒服很多**
> （但注意：**它会自己开回来**，每次模块重新联网前都得再关一次）。

---

## 5. 两个必须知道的时序坑（**本篇最值钱的两条**）

这两条是**实测踩出来的**，都是"指令明明回了 `OK`，但什么都没发生"这一类，
不点破的话能查一天。

### 5.1 `AT+NSOCO` 回的 `OK` **不代表连上了**

BC28 的 `AT+NSOCO` 是**异步**的：那句 `OK` 只表示"**请求已受理**"。
**真正的 TCP 建连完成，是由一条 URC 单独上报的**：

```
+NSOCO: <socket>
```

**在等到这条 URC 之前 `AT+NSOSD` 发出去的数据，会全部丢掉** —— 而且 `NSOSD` 照样回
`OK` 和 `<socket>,<长度>`，**看起来完全成功**。

**实测这个 URC 有多慢**（同一个国内 IP、同一个板子）：

| 那次 | 从 `NSOCO` 到 `+NSOCO` URC |
|---|---|
| 第一次 | 约 **2 秒** |
| 另一次 | 约 **22 秒** |

**所以判据只能是"等到 `+NSOCO: <n>` 这一行"，不能是"等 3 秒"、更不能是"看 OK"。**

> **这就是我一开始以为"连不上自己的 VPS"的原因**：数据在连上之前就发出去了，
> 对端收到一个空连接、超时断开 —— 表现出来像"网络不通"，其实是**发早了**。

### 5.2 `AT+NSORF` 也不是立刻就有数据

`NSOSD` 回 `OK` 只表示**本地发出去了**。对端的响应什么时候回来是**另一件事**，
NB-IoT 上**几秒到十几秒都正常**（§4.6 的 PSM/eDRX 会拉长它）。

- **要轮询** `AT+NSORF`，别指望第一条就读到。
- **别过早 `AT+NSOCL`** —— 关掉之后缓冲区跟着没了。
- 实测节奏：`NSOSD` 之后**每 3–5 秒轮一次 `NSORF`，连着轮 4–5 次**比较稳。

### 5.3 完整的、可照抄的一轮（**实测成功**）

目标：向一个国内的 TCP 服务器发一个 HTTP 请求并把响应读回来。
（下例把目标 IP 换成**你自己**的服务器即可；我这里用的是国内某站点的 80 口。）

```
AT+NSOCR=STREAM,6,0,1                    → 3            ← 返回的是"分配到的 socket 号"，不一定是 0
AT+NSOCO=3,110.242.69.21,80              → OK
                                          ← ⚠️ 必须等到这一行：+NSOCO: 3
AT+NSOSD=3,18,474554202f20485454502f312e300d0a0d0a   → 3,18 / OK
                                          ↑ 长度 18，后面是 18 字节的十六进制
AT+NSORF=3,128                           → 3,110.242.69.21,80,128,<128 字节的 hex>,1232 / OK
AT+NSORF=3,128                           → 3,110.242.69.21,80,128,<128 字节的 hex>,1104 / OK
AT+NSOCL=3                               → OK
```

**三个参数的读法**：

| 指令 | 参数 | 意思 |
|---|---|---|
| `AT+NSOCR=STREAM,6,0,1` | `STREAM` / `6` / `0` / `1` | 类型 TCP / 协议 TCP / 本地端口(0=自动) / **接收模式 1 = 数据靠 URC 提示，要用 NSORF 去取** |
| `AT+NSOSD=3,18,<hex>` | socket / 字节数 / 十六进制数据 | 发数据 |
| `AT+NSORF=3,128` | socket / 想读多少字节 | 收数据 |

**`AT+NSORF` 的回显要会拆** —— 它是**五个字段**：

```
3,110.242.69.21,80,128,485454502F312E3020323030204F4B...,1232
│ └─ 对端 IP ──┘ └端口┘ └本次读到的字节数┘ └─ 数据(hex) ─┘ └缓冲区里还剩这么多┘
└ socket 号
```

把上面那段 hex 解出来（`48 54 54 50` = `HTTP`）：

```
HTTP/1.0 200 OK
Accept-Ranges: bytes
Cache-Control: no-cache
Content-Length: 29506
Content-Type: text/html
Date: Sat, 12 Sep 2026 15:33:14 GMT
...
Server: ...
```

**这就是端到端跑通的证据**：模块真的把对方服务器的 HTTP 响应收回来了，
`Content-Length: 29506` 说明那是个两万多字节的页面，`剩余 1232` 说明缓冲区里还有。

> **`NSOCR` 返回的 socket 号要接着用**，它**不保证是 0**（我这次是 `3`，
> 因为 0/1/2 之前用过还没回收）。**每次都把回显里那个数字读下来再往下敲**，
> 猜错号会直接 `ERROR`。同理，`AT+NSOCL` 一个不存在/已关闭的 socket 也回 `ERROR` —— 无害。

---

## 6. 只查不发的指令（配置现状速查）

想快速摸清一块板子的当前状态，按顺序敲这 8 条就够：

```bash
python at_console.py "ATI" "AT+CGMR" "AT+CSQ" "AT+CEREG?" "AT+CGATT?" "AT+CGPADDR" "AT+CGCONTRDP" "AT+NUESTATS"
```

对应 §4 各表。**`AT+CGMR` + `AT+CGCONTRDP` + `AT+NUESTATS` 这三条信息量最大**：
固件版本、当前 APN、信号质量，一眼看完。

---

## 7. 本篇没打通 / 没定论（**照实说**）

| 事 | 状态 |
|---|---|
| **`AT+NPING`** | ⚠️ **没拿到结果**。`AT+NPING="114.114.114.114"` 回了 `OK`，但**没等到 `+NPING:` 那条结果 URC**；`AT+NPING="www.baidu.com"` 直接 `ERROR`（**看起来只吃 IP、不吃域名**）。原因没定论 —— 可能是它也要等 URC 而我等得不够久，也可能是这张卡/这个固件不放通 ICMP |
| **连境外的服务器** | ⚠️ **不稳定**。同一个境外 IP（我自己的 VPS，**IP 打码**），PC 上 `curl` 回 404 证明它是活的；模块这边 **4 次里只有 1 次等到 `+NSOCO` URC**。而**国内 IP 是 2 次全成**。**"境外那条路明显更不稳"这个结论可以做，但"境外一定不通"不能下** —— 它成功过一次，而且样本太小 |
| **境外那条路的收发** | ❌ 没验证。只等到过建连 URC，**没能把数据发过去并收到回**（两次都因为没等够时间就发了） |
| **`AT+NUESTATS` 的读数是变的** | ⚠️ 不是 bug，是事实。两次跑，**Cell ID / PCI 都不一样**（换了服务小区）。**别把小区号写死在代码里** |
| **从 NB-IoT 拿到的 IP 能不能被外面主动连** | ❌ 没验证。`+CGPADDR` 给的 `10.x` 是运营商大内网地址，**极可能不能**（NB-IoT 常规做法是模块主动连出去）。要做"服务器推给模块"，得另想办法 |
| **ICCID 与卡面印刷数字对不上？** | ⚠️ **没核对**。`AT+NCCID` 回的那个 ICCID，**没跟卡面上印的数字比过**。比一下是个很好的自检 —— 留给你 |
| **`AT+CGEREP?`** | ❌ 报 `ERROR`。`AT+CLAC` 的清单里有它，但**运行时用不了** |
| **`AT+CEDRXS=0`（不带参数）** | ❌ 报 `ERROR`。要关 eDRX 得带参数（`AT+CEDRXS=0,5` 之类）—— **这个没验证** |
| **`AT+CLAC` 的清单** | ⚠️ **只能当线索**。它列出的是固件里编译进去的指令，**不等于运行时都能用**（`AT+NUICC`、`AT+NIPINFO`、`AT+CGEREP?` 都列在里面，实际全报 `ERROR`） |
| **QNavigator（移远官方工具）** | ⬜ 没试。本篇走的是**自己写透传固件**这条路，比它更底层、更可控。想省事的读者可以直接用它 |

---

## 8. 本篇要跟哪几样东西一起发

| 一起发什么 | 为什么 | 在哪 |
|---|---|---|
| **① 本篇文档** | 就是这一份 | 仓库根目录 |
| **② 透传固件 `main.c`** | §2 那条路的实物。**没有它，本篇后面全部指令都敲不了** | `04-AT透传/` |
| **③ 固件和厂家模板的 diff** | §2.3 说"用这份 main.c 覆盖"，diff 让你看清覆盖掉了什么 | `04-AT透传/main.c.diff` |
| **④ `at_console.py`** | §3 的敲指令工具 | `02-工具/` |
| **⑤ 第 1 篇那套工具脚本** | 编译、烧录、看串口日志（§2.3 第 2 步要用） | `02-工具/` |
| **⑥ 厂家资料包** | 建工程要用的**例程 1** 在里面 | **只能跟卖家/作者要**（3.4 G 传不上仓库） |
| **⑦ 一张能用的 NB-IoT 卡 + IPEX 天线** | §1 就卡在这儿 | — |

---

## 9. 下一步（第 3 篇要做的）

本篇是**用 PC 敲指令**，模块在一次 PC 会话里被动响应。
第 3 篇要做的是**把 §5.3 那一轮流程写进固件**：上电 → 注网 → 建 socket → 定时把数据发到自己的服务器 → 睡。

已知会遇到的、本篇已经替你踩掉的：

1. **`NSOCO` 要等 URC**（§5.1）—— 固件里不能"发完 `AT+NSOCO` 就接着发数据"。
   厂家例程就是这么写的，**所以它大概率从来没真正发出过数据**。
2. **`NSORF` 要轮询、要有超时**（§5.2）。
3. **PSM/eDRX 出厂是开的**（§4.6）—— 定时上报的周期要按它给的延迟设计，
   否则会出现"服务器十几秒后才收到"这种诡异现象。
4. **服务器优先放国内**（§7）—— 境外那条路实测不稳。

> **"要不要用 `AT+NSOCO` 之外的方式"** 也是第 3 篇要定的：移远还有 `AT+NSOSTF`
> （带 flag 的发送，可以在发完顺带关闭）等变体，**本篇没验证过**，第 3 篇再实测。
