# STM32L151 + BC28 开发板 · 操作手册（第 3 篇：把数据发到自己的服务器）

> **系列目录 → [`README.md`](README.md)**

> ### 这是系列的第 3 篇
>
> **[第 1 篇](第1篇-搭好初始环境.md)** 跑通「编译 → 下载 → 串口打印」，
> **[第 2 篇](第2篇-BC28联网与AT指令实测.md)** 让 BC28 联上网、并用 PC 敲 AT 验证了收发，
> **[第 4 篇](第4篇-墨水屏点亮与显示实测.md)** 点亮了墨水屏。
> 本篇回到系列的主线：**把「人在 PC 上敲指令」变成「固件自己干」** ——
> 上电 → 注网 → 建 socket → 每 30 秒把一行数据发到自己的服务器。
>
> | 篇 | 主题 | 状态 |
> |---|---|---|
> | [第 1 篇](第1篇-搭好初始环境.md) | 搭好初始环境（编译 / 下载 / 看日志） | ✅ 已发布 |
> | [第 2 篇](第2篇-BC28联网与AT指令实测.md) | BC28 联网 + AT 指令实测 | ✅ 已发布 |
> | **第 3 篇** | **把数据发到自己的服务器（写进固件，不用 PC）** | ✅ **本篇** |
> | [第 4 篇](第4篇-墨水屏点亮与显示实测.md) | 墨水屏点亮 + 显示实测 | ✅ 已发布 |
> | [第 5 篇](第5篇-多路采集与云端看板.md) | 多路采集 + 云端看板 | ✅ 已发布 |
>
> **本篇的写法**（跟前几篇一样，先划清楚）：
>
> - **不用厂家的 `BC28.c`**，驱动整个自己写。厂家代码只在 **§3** 做逐条对照，**不参与编译**。
>   （跟第 4 篇不搬 `smp.c` 是同一个做法。）
> - **面板/时序/协议一律照抄手册或厂家**，改动过的地方一条条说明理由。
> - **我自己算出来和实测的数字分开写**；「推算」两个字会标出来。
> - **实验没做出来的结论，就写"没做出来"** —— §6 是一个我自己预测错、被实测打脸的完整记录。
> - 没做、没验证过的，一律进 **§9**。
>
> **实测环境**：2026-09-13，Windows 11 + Keil MDK 5.24a（编译器 V5.06u5）+ ST-Link V2 克隆版，
> `<COM 口>` = COM7。SIM 是移动 NB-IoT 卡，模块固件 `BC28CNVBAR02A03`。

**一句话结论**：**能跑，而且比预想的稳。**
固件上电后自己注网、建 socket、每 30 秒发一行，**一件事都不用人管**。
境外那台服务器上，**同一个 TCP 连接连续挂了 63 分钟、发出 110 条、服务端一条不落**。
但要走到这一步得先绕开三样东西：
**① 公网裸端口必须挡一层 token（§2）；② 厂家的 `BC28.c` 有三处不能照抄（§3）；
③ `AT+NSOSD` 的长度和 hex 两个细节（§4.3）。**

另外有两件事值得单独说：

- **我原本要证明的东西，没证明出来。** §6 那个 A/B 实验，我预测"不等 `+NSOCO` URC 就会丢数据"——
  实测**打脸**：数据一条没丢。而想比"哪个连接更稳"，跑与跑之间的方差又压倒了宏本身。
  这一节我按实验记录原样写，包括我预测错在哪。
- **"丢包"这个数，两边的计数器都会骗你。** 家里那轮串口自报 `lost 11`，服务端实收**一条不少**；
  短连接那轮串口自报 `lost 51`，服务端的 2 个缺口看着像丢包，其实**是 2 次建连失败**（§7.1）。
  两边的数都得会读 —— 这条在 **§7.1 / §8**，是本篇我觉得最值钱的两节。

---

## 0. 三步速查（熟了只看这一节）

```
① 骨架：拿第 2 篇那份已经跑通的工程（bc28-bridge：裁剪过、没有厂家 BC28 应用层）
② 换文件：
     06-BC28通信/{bc28.c, bc28.h}  → 工程里的 HARDWARE/bc28/
     06-BC28通信/main.c            → 工程里的 Project/Test/main.c
   .uvproj：两处 —— ① IncludePath 里加一段 ..\..\HARDWARE\bc28
                     ② HARDWARE 组里**新增**一条 bc28.c（跟第 4 篇"换掉 smp.c"不同，
                        这里是加，不是换）
③ 三处要改成你自己的：main.c 里的 SERVER_HOST / SERVER_PORT / SINK_TOKEN
   然后双击 flash_and_run.bat，同时看两处：
      串口 → 注网 + DNS 解析 + "+ TCP connected" + 每 30 秒一条 [n] #n,...
      服务端日志 → 每 30 秒多一行带时间戳的记录
```

服务端那几十行在 `06-BC28通信/sink.py`，**只用 Python 标准库**，拷到服务器上直接 `python3 sink.py` 就能跑。

---

## 1. 本篇要做什么、跟第 2 篇的分工

第 2 篇的成果是「**人在 PC 上敲 AT，证明这条链路能通**」。
但那条链路上有一个人：他得看着、得敲、得在 `+NSOCO` 来了之后才敲下一条。

本篇把那个人拿掉：

| | 第 2 篇 | 第 3 篇 |
|---|---|---|
| 谁在操作模块 | 人（PC + 串口助手） | **固件** |
| 建 socket / 发包 | 手敲 AT | `BC28_Open()` / `BC28_Send()` |
| 出错了怎么办 | 人看一眼，重来 | **固件自己判断 + 重连** |
| 能不能长期挂着 | 不能 | 能（这就是本篇要验的） |

**所以本篇真正要回答的问题是三个**：

1. 把那一串 AT 搬进固件，**第一枪能不能打通**？
2. 打通之后**能不能长期挂着**？挂多久？会不会掉？掉了怎么办？
3. 第 2 篇留了一个"坑"的说法——**"`AT+NSOCO` 回的 OK 只是受理，必须等 `+NSOCO` URC 才能发数据"**。
   这条到底成不成立？（§6，结论：**我没能证明它**。）

---

## 2. 接收端：服务器上那几十行

### 2.1 `sink.py` 的全部逻辑

接收端故意做到最小：**Python 标准库、单文件、不用框架、不用数据库**。
核心就这几件事：

```python
class Handler(StreamRequestHandler):
    timeout = 600                       # ← 这个数有个坑，见 2.3
    def handle(self):
        line = self.rfile.readline(1024)         # 1) 按行读第一行
        if not re.match(r"^#TOKEN\s+(\S+)$", line.decode().strip()):
            self.log("REJECT", peer, line_ascii) # 2) token 不对 -> 记一笔就断
            return
        self.wfile.write(b"AUTH OK\n")
        self.log("AUTH", peer, "token ok")
        while True:
            data = self.rfile.readline(1024)     # 3) 之后每行原样落盘
            if not data: break
            self.wfile.write(("OK %d\n" % bump()).encode())
            self.log("RX", peer, data.decode().strip())
```

日志是**定宽**的，一行一件事：

```
[2026-09-13 20:44:39 +08] AUTH   117.136.x.x  token ok
[2026-09-13 20:44:45 +08] RX     117.136.x.x  #1,CSQ=26,RSRP=-73.7,SNR=2.4,ECL=0,UP=7
```

| 类型 | 什么时候写 | 说明 |
|---|---|---|
| `AUTH` | 每条连接**成功通过 token** 时 | 一条连接只有一条。**所以"AUTH 的条数 = 对方重连了几次"** |
| `RX` | 每条通过鉴权的数据行 | 原样记录，服务端不解析业务内容 |
| `REJECT` | 第一行不是合法 token | 记下对端 IP |
| `INFO` | 连接结束 | ⚠ 有 600 秒的滞后，见 2.3 |

### 2.2 公网裸端口必须有 token

**这是 NB-IoT 项目里最容易漏掉的一件事。**

一旦你把 9101 端口映射到公网，它就在被全网扫描。没有 token 的话：

- 任何人往这个端口塞一行垃圾，你的日志里就会多一行——你分不清那是你的板子还是别人；
- 更糟的是，如果你的服务端"收到什么就按什么行事"（比如收到 `#RELAY ON` 就去动继电器），
  那就等于把控制权交给了扫描器。

所以规则很简单：**每条连接的第一行必须是 `#TOKEN <32 位随机串>`，不对就记一笔然后断开。**

```
#TOKEN 是**预共享**的, 不是协商出来的。它同时存在两边:
  服务器: sink.env (权限 600) 里的 SINK_TOKEN=...
  固件:   main.c 里的 SINK_TOKEN
```

> ⚠ **这个串不要提交到任何公开仓库。** 本篇正文和示例代码里一律写成占位符。
> 公开仓库里那条 `#define SINK_TOKEN "..."` 必须换成你自己的值。

### 2.3 一个我事后才发现的坑：`readline` 超时 600 秒

`sink.py` 里 `timeout = 600` 是"这一行 600 秒还没来就断开"。
它本意是防止一条死连接占着线程不放。**但它让日志变得会骗人。**

看这段真实日志：

```
[2026-09-13 20:35:23] RX     117.136.x.x  #7,...,UP=184      ← 最后一条
[2026-09-13 20:35:43] INFO   117.136.x.x  连接结束
```

`INFO` 打在 20:35:43，你很容易读成"这条连接活到了 20:35:43"。
**不是。** 20:35:43 减去 600 秒 = **20:25:43**，正好是 `#7` 那一刻——
也就是说**连接在 20:25:43 就已经静默死掉了**，服务端只是等到 `readline` 超时才察觉。

**静默死**（对端没发 FIN/RST，连接就这么没了）是 NAT 回收映射的典型特征。
所以：

> **判断"连接活了多久"，不能用 `INFO` 的时间戳减去 `AUTH` 的时间戳。**
> 要么把超时调小（我后来想改成 90 秒），要么只信 `AUTH` 和 `AUTH` 之间的间隔。

本篇后面的连接寿命，用的都是 **`AUTH` 到下一个 `AUTH`** 的间隔——这个数不受 600 秒影响。

---

## 3. 为什么不搬厂家的 `BC28.c`（逐条对照）

厂家那份 `HARDWARE/BC28/BC28.c` 一共 **283 行**，`BC28.h` **18 行**。
它不是不能用（厂家例程能跑），但**有三处不能照抄**。以下每一条都是原文核对过的。

### 3.1 建连判据：等 `OK`，而不是等 `+NSOCO`

厂家 `BC28_ConTCP()` 的骨架（原文，省略了关闭旧 socket 那三句）：

```c
Uart2_SendStr("AT+NSOCR=STREAM,6,0,1\r\n");
Delay(300);
strx = strstr((const char*)buf_uart2.buf, (const char*)"STREAM,6,0,1");
if(strx) socketnum = strx[14];              /* ← 见 3.3 */

sprintf(atstr, "AT+NSOCO=%c,%s,%d\r\n", socketnum, SERVERIP, SERVERPORT);
Uart2_SendStr(atstr);
Delay(300);
strx = strstr((const char*)buf_uart2.buf, (const char*)"OK");   /* ← 只找 OK */
errcount = 0;
while(strx == NULL) {
    errcount++;
    Delay(30);
    strx = strstr((const char*)buf_uart2.buf, (const char*)"OK");
    if(errcount > 10) { errcount = 0; break; }                  /* 最多约 0.33 秒 */
}
```

**这个文件里从来没有出现过 `+NSOCO` 这个串。**（`grep NSOCO` 只有两处：一句注释、
一句 `sprintf` 里的 `AT+NSOCO=`。没有一处是在等 URC。）

问题在于 `AT+NSOCO` 有**两段响应**：AT 回的 `OK` 只表示"命令受理了"，
真正建连完成是随后另发的一条 `+NSOCO: <socket>`。实测这条 URC 要 **3~20 秒**（§7）才来。

而厂家的循环只等 **约 0.33 秒**，而且等的还是 `OK`。所以它**必然在连接真正建好之前就返回**。

> **这就是本篇 §6 那个 A/B 实验要验的东西。** 提前说结论：**我把结果预测错了**，
> 详见 §6——它确实返回得太早，但数据居然没丢。

### 3.2 收数走 256 字节的 `buf_uart2`，满了**静默回绕**

厂家收串口数据用的是 `usart.c` 里的全局缓冲：

```c
#define BUFLEN 256                              /* usart.h:9 */
UART_BUF buf_uart2;                             /* usart.c:25 */

void nbiot_receive_process_event(unsigned char ch) {
    if(buf_uart2.index >= BUFLEN) {
        buf_uart2.index = 0 ;                   /* ← 满了从头覆盖 */
    } else {
        buf_uart2.buf[buf_uart2.index++] = ch;
    }
}
```

两个问题：

1. **只有 256 字节。** 等 `+NSOCO` URC 那几秒里，模块可能已经在往回吐东西；
   在 9600 波特下 256 字节约 **0.27 秒**就填满（推算：256 × 10 bit ÷ 9600 ≈ 0.27 s）。
2. **填满了是从头覆盖，而且不留任何标记。** 于是你 `strstr` 的可能是被截断的、
   甚至是拼接过的一条"回显"——**它可能在中间某处伪装成一条合法的 `OK`**。

我这份驱动收数走**自己的线性缓冲**（1024 字节），满了**不覆盖、只记一个 `rx_ovf` 标记**，
并且提供一个 `BC28_RxOverflow()` 让上层能查。

### 3.3 `socketnum = strx[14]`：硬数第 14 个字符

```c
strx = strstr(buf_uart2.buf, "STREAM,6,0,1");
if(strx) socketnum = strx[14];
```

它假定 `AT+NSOCR` 的回显里 socket 号**恰好落在第 14 个字符**上。
回显多一个空格、少一个空格、模块固件版本不同，这里就取错——
**而且取错之后没有任何检查，`socketnum` 是全局变量，会拿上一次的旧值继续用。**

我这份是**扫数字**（`take_num()` / `num_after()`），不依赖位置；

### 3.4 两点更正：我上一轮记错了

写这篇之前我重新核了一遍厂家代码，有两条我原先记的**是错的**，在这里更正——
因为本篇会对厂家代码做断言，不能凭印象写：

| 我原先记的 | 核对结果 |
|---|---|
| "厂家每种状态循环 100 次 × 300ms = 30 秒" | ❌ **只有 `BC28_Senddata()` 是这样**（`errcount>100`，`Delay(300)`）。`BC28_ConTCP()` 是 `errcount>10`、`Delay(30)`，**约 0.33 秒**。两者差 100 倍。 |
| "厂家 `BC28_Senddata()` 的长度和 hex 两点都错" | ❌ **厂家例程的调用是对的**。原文是 `BC28_Senddata("13","7777772e637367736d2e636f6d")`：`"13"` 是原始字节数、26 个 hex 字符解出来正好是 `www.csgsm.com`（13 字节）。**两点都对。** |

> 这条更正本身也是本篇的一部分：**做代码对照时，断言必须回原文核，不能靠记忆。**
> 我上一轮就是靠记忆，记错了两次。

---

## 4. 我写的驱动（`bc28.c` / `bc28.h`）

`bc28.c` **1096 行**（其中代码 **714 行**），`bc28.h` **108 行**（代码 **56 行**）。
比厂家那份（283 + 18 行）长，多出来的部分主要是**注释、错误码和边界检查**。

### 4.1 三层结构

```
┌─ 应用层  main.c ──────────────────────────────────────────────┐
│  link_up()     建连 + 交 token                                 │
│  report_once() 组一行数据、发出去、读回执                        │
│  wait_reply()  ← ⚠ "收不到就再试几轮" 的**策略**在这里            │
└───────────────────────┬───────────────────────────────────────┘
┌─ 驱动层  bc28.c ──────┴───────────────────────────────────────┐
│  BC28_Recv()   一 次 AT+NSORF，收到就返回                       │
│  at_cmd()      发一条 AT（先清缓冲）再等                         │
│  wait_for()    在收缓冲里等一个固定串，等到就返回                 │
│  read_field()  读到下一个换行为止（给"结果另发一条"的 AT 用）      │
└───────────────────────┬───────────────────────────────────────┘
┌─ 硬件层 ──────────────┴───────────────────────────────────────┐
│  rx_pump()     把 USART2 收到的字节搬进自己的线性缓冲             │
└───────────────────────────────────────────────────────────────┘
```

**这个分层在 §8 会很重要**：`lost` 那个计数器是**应用层**（`main.c`）自己发明的概念，
驱动里根本没有"丢包"这回事——`BC28_Recv()` 只回答"这一枪有没有读到"。

接口一览（`bc28.h`）：

| 分类 | 函数 | 说明 |
|---|---|---|
| 底层 | `BC28_Init()` / `BC28_Idle(ms)` / `BC28_Millis()` | 时基 + 空转（一边等一边收 URC） |
| 模块 | `BC28_Probe()` | `AT` → `ATE0` → `ATI`，返回模块型号与固件版本 |
| 模块 | `BC28_WaitNet(to_ms)` | 轮询 `AT+CEREG?` 直到注网 |
| 模块 | `BC28_GetRadio(&r)` | `AT+NUESTATS` + `AT+CSQ` |
| socket | `BC28_Resolve(host, ip, cap)` | **域名 → IP**，用模块自带的 DNS（§4.4） |
| socket | `BC28_Open()` / `BC28_Connect()` / `BC28_Send()` / `BC28_Recv()` / `BC28_Close()` | `NSOCR` / `NSOCO` / `NSOSD` / `NSORF` / `NSOCL` |
| 诊断 | `BC28_SocketClosed()` / `BC28_RxOverflow()` / `BC28_NsocoDelayMs()` / `BC28_Why()` / `BC28_DumpRx()` | 全是给"出错时知道该看哪儿"用的 |

返回值约定：**`>= 0` 成功，`< 0` 是错误码**：`-1` 参数错 / `-2` 超时 / `-3` 模块回 `ERROR` /
`-4` 没分到 socket / `-5` 回了 OK 但内容解析不出来。`BC28_Why(rc)` 把它翻成能直接打日志的短串。

### 4.2 超时取值的理由

`bc28.h` 里这几个数**不是拍脑袋的**，每个都有出处：

| 常量 | 值 | 依据 |
|---|---|---|
| `BC28_TO_AT` | 1000 ms | 普通 AT 指令 |
| `BC28_TO_LONG` | 2000 ms | 要翻一翻的指令 |
| `BC28_TO_STATS` | 5000 ms | `AT+NUESTATS` 是本模块回得最慢的一条 |
| `BC28_TO_NSORF` | 5000 ms | 单次 `AT+NSORF` 轮询 |
| `BC28_TO_URC` | 30000 ms | `+NSOCO` URC 实测 3~20 秒（§7），给足余量 |
| `BC28_TO_NET` | 120000 ms | 冷启动注网要重新找小区 |

对照一下：厂家在 `BC28_Senddata()` 里等 `OK` 等的是 **100 × 300ms = 30 秒**，
而且**循环里既不重发、也不清缓冲**——等不到就是干等。

### 4.3 `AT+NSOSD` 的两个细节

`AT+NSOSD=<socket>,<len>,<hexdata>`：

1. **数据要转成大写 hex。** 发 `"#1,hi\r\n"` 就要写成 `2331 2C68 690D 0A`。
2. **`<len>` 是原始字节数，不是 hex 字符数。** `"#1,hi\r\n"` 是 6 字节，
   `<len>` 就写 **6**，虽然 hex 串有 12 个字符。

驱动里把这两件事包在 `BC28_Send()` 里，上层只管给原始字符串和它的长度：

```c
int BC28_Send(int sock, const char *buf, uint16_t len)
{
    static char cmd[BC28_TXMAX * 2 + 32];       /* ⚠ 必须 static */
    int n = sprintf(cmd, "AT+NSOSD=%d,%u,", sock, (unsigned)len);
    for (i = 0; i < len; i++)
        n += sprintf(cmd + n, "%02X", (unsigned char)buf[i]);
    ...
}
```

> ⚠ **那个 `static` 不是随手写的。** 这个工程的 `Stack_Size = 0x400`（**1 KB**），
> 而 `cmd` 最长 `192×2+32 = 416` 字节。放在栈上会**直接踩穿栈**，
> 现象是"发到某一条就死机"，而且很难查。编译不报错，运行时才炸。

### 4.4 `AT+QDNS`：结果**跟在 OK 后面另发一条**（本篇踩的新坑）

要长期挂着，服务器地址就不能写死 IP——家宽的出口 IP 是动态的，光猫重启一次就变。
好消息是**这件事不用自己做**：模块自带 DNS 解析，就是 `AT+QDNS`。

我照着手册实现，第一版跑出来是这样：

```
RX> ! DNS: cannot resolve <域名>: parse-failed
```

于是把模块的原话打出来（这一步是定位的关键，别省）：

```
RX>   line<\n\nOK>
```

**回了 OK，但里面没有 `+QDNS:`。** 回头翻手册那一页的 Example，原文写的是：

```
AT+QDNS=0,www.baidu.com
OK                        ← 只是"受理了"（最大响应时间 300ms）
                          ← 空行
+QDNS:111.13.100.91       ← 结果**另发一条**
```

**又是一个"OK 只表示受理"的坑——跟 `AT+NSOCO` 一模一样的形状。**
我按"等到 OK 时整行已经在缓冲里"写，拿到的必然是一个光秃秃的 `OK`。

还有**第二层**：`wait_for()` 是"一命中期望串就返回、并且只消费到命中末尾"，
所以 `wait_for("+QDNS:")` 命中时，缓冲里只剩 IP 那半截——**得自己接着读到换行**。
为此加了一个 `read_field()`。

所以正确的顺序是**等两次、读两段**：

```c
rc = at_cmd(cmd, "OK", BC28_TO_LONG, NULL, 0);        /* ① 受理了，300ms 就回 */
rc = wait_for("+QDNS:", BC28_TO_URC, NULL, 0);        /* ② 结果到了 */
n  = read_field(out, cap, BC28_TO_LONG);              /* ③ 冒号后面那半截是 IP */
```

**超时怎么给**：第 ① 段是"受理"，手册说 300ms，给 2000ms 有余量；
**第 ② 段才是真正的 DNS 解析，要 2~3 秒**，所以必须用 URC 那一档（30 秒）。
第 ③ 段和 `+QDNS:` 通常同一批字节到，2000ms 够。

上层就更简单了——**写域名还是写 IP 都行，固件自己判断**：

```c
#define SERVER_HOST     "你的域名或IP"      /* 看着像 IPv4 就直接用, 否则先解析 */
```

> **这一步的价值**：固件里不需要塞 DNS 客户端（几百行，还要处理 UDP 重传），
> 不需要给模块开 IPv6，也不需要额外挂 DDNS 客户端——
> 只要域名那边有个**带 A 记录**的 DDNS，固件每次重连都会拿到最新 IP，**一个字节都不用重烧**。

---

## 5. 实测一：长连接

固件：`MODE_LONG = 1`（建一次连接，之后一直用），上报间隔 30 秒。
**服务器地址：先用域名（验证 §4.4 的 DNS），再写 IP。**

### 5.1 境外那台（直接挂在公网上）

串口：

```
+ DNS: <域名> -> <解析出的 IP>   (module's own AT+QDNS)
+ TCP connected <IP>:9101  socket=1  (+NSOCO URC came after 2189 ms)
+ server: AUTH OK
[1] #1,CSQ=13,RSRP=-95.8,SNR=12.7,ECL=0,UP=7
```

服务端日志（AUTH/INFO 时间线）：

```
[20:44:36] AUTH   117.136.x.x  token ok
[21:47:52] INFO   117.136.x.x  连接结束
```

**中间一条 `AUTH` 都没有。** 也就是 `#1` 到 `#110` **全部跑在同一个 TCP 连接上**：

| | 数值 |
|---|---|
| 单连接寿命 | **63 分 16 秒** |
| 上报条数 | **110**（`#1` → `#110` 连续） |
| 丢包 | **0** |
| 板子侧 `UP` 递增 | 严格 30 秒一跳（7 → 3281） |

> ⚠ **这个数我中途记错过一次。** 直播到 30 分钟时我采样了一次日志，记下"30分38秒 / 65 条"，
> 就以为跑完了。**板子其实一直在跑。** 看 `AUTH/INFO` 时间线才知道那个连接挂到了 63 分钟。
> 教训：**"跑了多久"要用连接的开始和结束时间算，不能用"我看了多久"。**

### 5.2 家里那台（在两层 NAT 后面）

同样的固件，把 `SERVER_HOST` 换成一个 IP，指向家里那台小主机（光猫 → 路由器 → 主机，
中间两层 NAT，入站映射到 9101）：

| | 数值 |
|---|---|
| 单连接寿命 | **2 分 ~ 8 分半**，反复断 |
| AUTH 次数 | **14**（约 35 分钟内） |
| 上报条数 | **69**（`#1` → `#69` 连续） |
| 丢包 | **0** |
| `+NSOCLI` | **12 次**，串口抓到了 |

关键是这一行串口日志：

```
! got +NSOCLI: socket closed by network, reconnecting
```

`+NSOCLI` 是**模块告诉我们"socket 被关掉了"**。也就是说——
**这 12 次断开是网络侧发起的，不是固件自己判断错了。**

（顺带确认了一件事：`sock_closed` 这个标志在 `BC28_Connect()` 里会清零，
所以它不会"卡在 1"导致每轮都误报。这一点专门验过。）

### 5.3 两边对比，以及**为什么不能下"境内外"的结论**

| | 境外（直接挂公网） | 家里（两层 NAT 后面） |
|---|---|---|
| 单连接寿命 | **63 分钟** | 2 ~ 8.5 分钟 |
| AUTH | 1 | 14 |
| 上报 | 110 | 69 |
| 丢包 | **0** | **0** |

**看起来"境外更稳"，但这个结论我不下。** 理由：

1. **真正不同的变量不是"境内外"，是"sink 在不在 NAT 后面"。**
   家里那台在两台 NAT 设备后面（光猫 + 路由器），境外那台直接暴露在公网。
   要证"是 NAT 导致的"，得把 sink 换个位置再测一次——**本篇没做**。
2. **跑与跑之间的方差极大。** 同一个宏、同一台服务器，20:44 那次连接挂了 **63 分钟**，
   22:42 那次 **10 分钟里断了 2 次**（8分16秒 + 2分钟）。见 §6 的表。
3. **两边都是 0 丢包。** 连接断得多不多，和"数据到没到"是**两件事**——
   固件每次断都重连了，序号一直是连续的。

**能确定的只有一句**：这条链路上，**断连是常态，丢包不是**。
固件处理断连的方式（`+NSOCLI` → 关掉 → 重连）是work的。

---

## 6. 实测二：等不等 `+NSOCO` URC 的对照 —— **我预测错了**

### 6.1 实验设计

既然驱动是自己写的，"发完 `AT+NSOCO` 等不等 URC"就是一个**自己固件里的宏**：

```c
#define BC28_SKIP_NSOCO_URC  0    /* 0 = 等到 +NSOCO: 才算连上（正常） */
                                  /* 1 = 收到 OK 就当连上了，立刻发（复现厂家写法） */
```

**A/B 对照**：只改这一个数，重编译、重烧，其余全一样（同一份固件、同一台服务器、
同一个端口、同样的 30 秒间隔）。

**我的预测**（写在计划里的原话）：**B 组（不等 URC）→ 服务器 0 行新数据。**

理由：`AT+NSOCO` 的 `OK` 只是受理，URC 才是建连完成；在 URC 之前 `AT+NSOSD` 送出去的数据
应该全丢。这也是第 2 篇留下的那条结论。

### 6.2 实测结果

**B 组（`BC28_SKIP_NSOCO_URC = 1`，境外，9 分钟）串口：**

```
+ NSOCO got OK, assuming connected (socket=2) -- vendor style
! no AUTH OK (noted, going on anyway)
  (late) server: AUTH OK -- that was the handshake, not this one
  <- OK 117   (server OK 1, lost 0)
  <- OK 118   (server OK 2, lost 0)
  <- OK 119   (server OK 3, lost 0)
```

**服务端：收到 19 条，`#1` → `#19` 连续，丢 0。**

**预测错了。数据一条都没丢。**

| 组 | 宏 | 时长 | AUTH 次数 | 收到条数 | 丢包 |
|---|---|---|---|---|---|
| **B** | `SKIP=1`（厂家写法） | 9 分钟 | **7** | 19（#1→#19） | **0** |
| A | `SKIP=0`（等 URC） | 10 分钟 | **3** | 23（#1→#23） | **0** |
| A' | `SKIP=0`（同时段参照） | 63 分钟 | **1** | 110（#1→#110） | **0** |

### 6.3 为什么没能证明，以及一个可能的解释

**原因一：我以为在比的量，其实不是宏决定的。**

看 A 组那一行：**同一个宏（`SKIP=0`），20:44 那次挂 63 分钟，22:42 那次 10 分钟断 2 次。**
**跑与跑之间的方差（1 / 3 / 7 次 AUTH）压倒了两个宏之间的差别。**
想要比出"哪个更稳"，得在**同一个时段内交替跑 A/B/A/B**，不能分时段各跑一次——
时段是这里压不住的混淆变量。

**原因二（更根本）：模块会把数据缓存到连接建立之后再发。**

B 组里，`#TOKEN` 是在收到 `OK` 之后**立刻**发出去的，而服务端**收到了** `AUTH token ok`。
合理的解释是：**BC28 在 socket 还没连上时，并不把 `NSOSD` 的数据丢掉，而是先缓着，
等连接真正建成再发出去。** 如果是这样，"不等 URC 就会丢数据"这条**本身就不成立**。

### 6.4 所以第 2 篇那条结论错了？

**不能这么说。** 第 2 篇是在**透传固件上手工敲 AT**观察到的现象，条件和本篇不同。
本篇能确定的只是一句：

> **在本篇这份固件、这条链路、这个时段上，不等 `+NSOCO` URC 并没有丢数据。**

至于第 2 篇当时看到的是什么，**本篇没有定论**，留在 §9。
"等 URC"这个做法本身仍然是对的（更稳妥、且能拿到 `BC28_NsocoDelayMs()` 这个实测数据），
但**"不等就丢数据"这个理由，本篇不支持。**

> 这一节我原本打算写成"最值钱的一节"，最后写成了一个**否定自己预测**的记录。
> 我认为照实写比硬凑一个结论有用——**A/B 跑之前我当然觉得自己是对的。**

---

## 7. 实测三：短连接与建连耗时分布

固件：`MODE_LONG = 0`，每轮 **建连 → 发 → 收 → 关**，专门用来量建连耗时。

这里的"建连耗时"指的是 **`AT+NSOCO` 的 `OK` 到 `+NSOCO:` URC 之间那一段**——
也就是"命令受理了"到"真正连上"之间到底要多久。这是第 2 篇那条"2~22 秒"在固件里的精确版本。

**115 轮实测（境外，9600 波特）。** 串口自己攒着累计值，跑满之后读出来就是这一行：

```
[connect #115] 6993 ms   (min 2821 / max 20227 / avg 5575, n=115)
```

| 统计 | 值 |
|---|---|
| 最小 | **2821 ms** |
| 最大 | **20227 ms** |
| 平均（n=115） | **5575 ms** |

**读法**：

- **典型值是 3~8 秒**，不是 2 秒。第 2 篇记的"2~22 秒"里，下限 2 秒本篇没再见到
  （115 轮的最低是 2.82 秒）；上限 20.2 秒见到了，但 **115 轮里只出了 1 次**。
  **下限 2 秒大概是当时手工敲 AT 时的读数误差。**
- **这是一个"长尾"分布**：绝大多数落在 3~8 秒，偶尔跳一次 20 秒。
  所以超时**不能按平均值给** —— `BC28_TO_URC` 给 30 秒是对的，
  按平均值（5.6 秒）给的话，那一次 20 秒就会误判成失败。

### 7.1 ⚠ 这一节我一开始把"建连失败"记成了"丢包"

跑满 35 轮那次采样，我写的是"服务端 `#1`→`#40` 缺 `#32` 一条，**丢 1 条**"。

**这个判据是错的。** 跑满 115 轮再看，缺的是 **`#5` 和 `#32` 两个号** —— 看起来更像丢包了。
但把串口上那两个计数器放在一起，结论**反过来**：

| 计数器 | 在 `main.c` 里的位置 | 值 |
|---|---|---|
| `seq` | 循环第一行，**每轮都加** | **118** |
| `conn_n` | **只在建连成功之后**才加 | **116** |

`seq - conn_n = 2` → **有 2 轮压根没连上**：`link_up()` 返回失败，走了
`- connect failed, retry in 3 s` 那条分支，**`report_once()` 根本没被调用过**。

而这 2 轮，**正好就是服务端看到的 2 个缺口**（`4→6` 和 `31→33`）：

```
服务端序号 1..118, 实收 116 条, 缺 #5 和 #32
串口       seq=118, conn_n=116      ->  2 轮建连失败
2 == 2     ->  成功发出去的 116 条, 一条不少
```

**所以短连接这一轮的丢包是 0，不是 1。**

那两个号是**没发出去**，不是**发出去了没到** —— 而这两种情况在服务端日志上
**长得一模一样**。只有把 `seq` 和 `conn_n` 分开数才分得开。

> 这跟 §8.1 那个"串口 `lost` 不可信"是**同一个教训的两半**：
> 串口那个计数器会虚报，**服务端的缺口也不能直接当成丢包**。
> 两边都得会读，缺一不可。

顺带得到建连失败率：**116/118 = 98.3%**，大约**每 60 轮有 1 轮连不上**。
（这个数只在短连接模式下有意义 —— 长连接不是按"轮"重连的。）

**短连接模式的代价**：每轮要重新建连**平均 5.6 秒**，而预算只有 30 秒 ——
**约 19% 的时间窗花在"重新连上"上**，最坏那次 20.2 秒直接吃掉 67%。
对照 §5：长连接就算在家里那种 2.5 分钟被掐一次的最差环境里，也只有约 4%。

> **短连接的一个真实好处**：它天生免疫 §5 那个"连接被悄悄掐断"的问题——
> 每轮都新建，掐不掐无所谓。代价是每 30 秒白等几秒，以及**移动网络的信令开销**
> （每次建连都是一次真正的 TCP 握手 + 一次 `AT+QDNS`）。

---

## 8. 失败判据（对号入座，别瞎试）

**这一节是本篇最值钱的部分**，因为里面有一条是"**你自己的计数器在骗你**"。

### 8.1 ⚠ 串口报的 `lost` 不可信

先看两组实测：

| 实验 | 串口自报 | 服务端实收 | 真相 |
|---|---|---|---|
| 家里长连接（35 分钟） | `lost 11` | `#1`→`#69` **连续** | **丢 0 条** |
| 短连接（115 轮） | `lost 51` | `#1`→`#118`，缺 `#5`/`#32` | **丢 0 条**（那两个是建连失败，见 §7.1） |

**虚报得很离谱**（11 → 0，51 → 0）。短连接那轮里串口说"**115 轮有 51 轮没等到回执**"
（44%），而服务端**一条不少全收到了**。

旁证是服务端那个 `OK <n>` 计数器：
家里那轮里，`#1` 的回执是 `OK 31`、`#64` 的回执是 `OK 93`——
31 到 93 是 **62**，而 `#2`→`#64` 是 **63** 条。**服务端几乎全收到了。**

**为什么会虚报？** 看 `lost` 是怎么来的：

```c
/* main.c —— 应用层 */
static int wait_reply(int s, char *out, uint16_t cap, int rounds)
{
    for (i = 0; i < rounds; i++) {          /* rounds = 3 */
        n = BC28_Recv(s, out, cap, BC28_TO_NSORF);
        if (n > 0) { trim(out); return n; }
        ...
        BC28_Idle(1000);
    }
    return 0;                                /* 3 轮都没有 -> 记一笔 lost */
}
```

`rounds = 3` 是**应用层的策略**，驱动里根本没有"丢包"这个概念——
`BC28_Recv()` 只回答"这一枪有没有读到"。

而 NB-IoT 模块在 **eDRX / PSM** 里睡着的时候（第 2 篇查过，出厂就开着），
服务端的回复本来就会迟到几十秒。**`wait_reply` 量的是"我没等到"，不是"对方没收到"。**

> **正确的丢包判据只有两个**：
> **① 服务端日志里序号的连续性；② 报文里的 `UP` 列。**
> 看到串口 `lost` 和服务端日志对不上，**先怀疑判据，别先怪链路。**

### 8.2 `UP` 列：唯一能分清"谁的问题"的尺子

每个报文里都带一个 `UP` 字段，值是**板子自己的 uptime（秒）**——
来自 `BC28_Init()` 里那套毫秒时基，除以 1000：

```
#12,CSQ=28,RSRP=-68.3,SNR=6.4,ECL=0,UP=360
                                      ↑ 板子上电后 360 秒
```

它的用处：**服务端收到时间 − UP = 真正的投递延迟。** 于是

- 服务端时间戳有 90 秒空档，但 `UP` 仍然 30 秒一跳 → **板子一直准点发，滞后在网络侧**；
- 服务端时间戳有 90 秒空档，`UP` 也一跳 90 秒 → **板子自己卡了**。

**没有这一列，光看服务端时间戳这两种情况分不开**，很容易把网络排队误判成"固件卡死"。

它同时还是个**重启探测器**：`UP` 突然从 1900 掉回 7，说明板子重新上电了。

### 8.3 对号入座表

| 现象 | 大概率原因 | 先看哪儿 |
|---|---|---|
| 串口一个字节都没有 | 漏了 `uart1_init(9600)` | `printf` 走的是 USART1，不是模块那条 USART2 |
| `AT` 打了几十次没回应 | 模块在 PSM 里睡着，第一个字符丢了 | 正常，`BC28_Probe` 的重试循环就是为它写的；`BC28_DumpRx()` 看有没有字节 |
| `! NSOCO: no +NSOCO URC` | 建连真的超时了 | `BC28_DumpRx()` 看模块最后说了什么 |
| `! DNS: ... parse-failed` | 把 `+QDNS:` 当成和 `OK` 同一条了 | **§4.4**：`OK` 只是受理，结果另发一条 |
| 服务端 0 行日志，但抓包显示字节都到了 | **发出去的字符串没有行尾 `\r\n`** | `sink.py` 是 `readline()`，没换行它会一直等 |
| 串口说 `lost N`，服务端说收到了 | **判据错，不是链路错** | **§8.1** |
| 服务端 `INFO` 的时间和"最后一条数据"差了正好 600 秒 | `readline` 超时 | **§2.3**：连接早就静默死了 |
| 发到某一条就死机 | 大缓冲放在栈上，踩穿了 1KB 的栈 | **§4.3**：大缓冲一律 `static` |

---

## 9. 本篇没做 / 没定论（**照实说**）

1. **"境外比家里稳"这个结论没做出来。** 看起来是（63 分钟 vs 2~8.5 分钟），
   但真正的变量可能是"sink 在不在 NAT 后面"。**没做**：把家里那台挪到公网、或把境外那台
   放到 NAT 后面，再测一次。
2. **`SKIP_NSOCO_URC` 的 A/B 没有定论**（§6）。跑间方差压倒了宏效应。
   **没做**：同一时段内交替 A/B/A/B，各跑足够多轮。
3. **第 2 篇"不等 URC 会丢数据"这条，本篇不支持，但也没推翻。**
   可能的原因是模块会缓存数据（§6.3），**这只是一个解释，没有直接证据**
   （要拿到证据得去抓模块的 `NSOSD` 在 URC 之前的行为，本篇没做）。
4. **家里那条路径为什么每 2~8 分钟断一次，没有定论。**
   串口证明了是网络侧发起的（`+NSOCLI`），但"是哪一层 NAT、按什么规则回收"**没有证据**。
5. **`readline` 超时 600 秒这个坑没修。** 事后才发现它让日志的 `INFO` 滞后 600 秒（§2.3）。
   改法是把这个数调小（比如 90 秒），**本篇没改**——改了要重测一遍全天数据。
6. **短连接模式的移动网络信令开销没量。** 每 30 秒建一次 TCP，对卡和基站意味着什么，
   本篇只有推断，没有数据。
7. **没做**：断网重连、模组重启、服务器重启这些异常场景的自动化测试。
   本篇的"重连"都是被网络掐断后自然触发的，不是有意构造的。
8. **短连接那 2 次建连失败没查原因。** 只从 `seq - conn_n = 2`（§7.1）知道有 2 轮没连上，
   **模块当时回了什么没有记录** —— 那两轮的串口输出我没留。
   要查得等下一次失败时抓 `BC28_DumpRx()` 的原始回显。

---

## 10. 下一步（第 5 篇）

> **📌 第 5 篇已发布** → [`第5篇-多路采集与云端看板.md`](第5篇-多路采集与云端看板.md)。
> 下面这份是当时列的设想，**实际做的跟它有几处出入**，照实记一下：
>
> | 当时想的 | 实际做的 |
> |---|---|
> | DHT11 温**湿**度 | **只要温度** —— 手上那颗湿度通道恒为 0（校验和对，是它真在发 0），第 5 篇 §7.2 |
> | 用 **Cell ID 反查**坐标 | ❌ **没做**，页面上那一行空着。板子上没有 GPS，第 5 篇 §10.3 |
> | 服务端"只落盘不解析"就够 | ⚠️ **不够** —— 要画历史曲线就得能查，第 5 篇 §9 给接收端加了 **SQLite**（文本日志照旧） |
> | 只做采集 | 中间还插进来一个**大坑**：BC28 的 socket 层会自己卡死，`CSQ` 却完全正常，只有 `AT+NRB` 能救，第 5 篇 §8 |
> | 「必须一起修的」`LED.c` 模拟模式 | ⬜ **仍未修**，仍挂在第 5 篇 §12 |

第 5 篇要做的是**把"多路采集 + 云端看板"补上**，本篇已经把管子铺好了：

- **采集**：DHT11 温湿度（板子上有排针座，**模块是 3 针小板，直接插上，不用焊**）、
  ADC1_IN16 读 CPU 温度、`AT+QCHIPINFO` 读模块温度与电池电压。
- **上报格式**：本篇那行 `#n,CSQ=..,RSRP=..,SNR=..,ECL=..,UP=..` 直接往里加字段就行，
  **服务端只落盘不解析**这个设计在这里就省事了（加字段不用改服务端）。
- **坐标**：用 Cell ID 上报，**反查放在服务端做**。
- **看板**：⚠ **不能做成公网可以直接打开、还带着地理位置的页面。**
- **必须一起修的**：厂家 `LED.c` 里把 PA8/11/12/15 配成了 `GPIO_Mode_AN`（模拟模式）。

---

## 11. 本篇要跟哪几样东西一起发

| 目录 / 文件 | 内容 |
|---|---|
| `06-BC28通信/bc28.c`、`bc28.h` | 自己写的驱动（§4） |
| `06-BC28通信/main.c` | 本篇的应用层（`MODE_LONG` / `SKIP_NSOCO_URC` / `REPORT_MS` 三个开关都在里面） |
| `06-BC28通信/main.c.diff` | 相对第 2 篇那份透传固件的改动 |
| `06-BC28通信/Project.uvproj.diff` | 工程改动（IncludePath + 新增 `bc28.c`），**就两个 hunk** |
| `06-BC28通信/sink.py` | 服务端，Python 标准库单文件（§2） |
| `06-BC28通信/deploy_sink.sh` | 在服务器上一键装 systemd 服务 + 生成 token + 放行端口 + 自检 |
| `06-BC28通信/README.md` | 怎么部署、怎么自检、日志在哪 |

> ⚠ **发布前必须处理掉的**：`SINK_TOKEN` 的真值、服务器的真实 IP 和域名、
> 模块的 IMSI / IMEI / ICCID / 内网 IP、`_bc28test.log` 这类带模块标识的日志。
> 公开仓库里 `SERVER_HOST` / `SERVER_PORT` / `SINK_TOKEN` 一律是占位符。
