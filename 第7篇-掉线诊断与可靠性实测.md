# STM32L151 + BC28 开发板 · 操作手册（第 7 篇：掉线诊断与可靠性实测）

> **系列目录 → [`README.md`](README.md)**

> ### 这是系列的第 7 篇
>
> **[第 1 篇](第1篇-搭好初始环境.md)** 跑通「编译 → 下载 → 串口打印」，
> **[第 2 篇](第2篇-BC28联网与AT指令实测.md)** 让 BC28 联上网、并用 PC 敲 AT 验证了收发，
> **[第 3 篇](第3篇-把数据发到自己的服务器.md)** 把「人敲指令」变成「固件自己发」，
> **[第 4 篇](第4篇-墨水屏点亮与显示实测.md)** 点亮了墨水屏，
> **[第 5 篇](第5篇-多路采集与云端看板.md)** 加了温度、修了 socket 卡死、把数据变成了网页。
> 本篇不再加新功能 —— 它只干一件事：**把"板子为什么半夜老掉线"这件事查清楚。**
>
> | 篇 | 主题 | 状态 |
> |---|---|---|
> | [第 1 篇](第1篇-搭好初始环境.md) | 搭好初始环境（编译 / 下载 / 看日志） | ✅ 已发布 |
> | [第 2 篇](第2篇-BC28联网与AT指令实测.md) | BC28 联网 + AT 指令实测 | ✅ 已发布 |
> | [第 3 篇](第3篇-把数据发到自己的服务器.md) | 把数据发到自己的服务器 | ✅ 已发布 |
> | [第 4 篇](第4篇-墨水屏点亮与显示实测.md) | 墨水屏点亮 + 显示实测 | ✅ 已发布 |
> | [第 5 篇](第5篇-多路采集与云端看板.md) | 多路采集 + 云端看板 | ✅ 已发布 |
> | 第 6 篇 | 省电（两档开关 + MCU STOP） | ⏳ 验证中，稍后发布 |
> | **第 7 篇** | **掉线诊断 + 可靠性实测** | ✅ **本篇** |
>
> **本篇的写法**（跟前几篇一样，先划清楚）：
>
> - **本篇最重要的三段，写的是"我判断错了"。** 两次误判各占一节（§2.4、§5），
>   而且**第二次误判造成的停机比它要治的病还长**。这部分不删。
> - **"我以为"和"实测是"分开写**；「推算」两个字会标出来。
> - **没查清的就写"没查清"** —— §9 是完整的「没做 / 没定论」清单。
> - 没验证过的结论一律不进正文，进 §9。
>
> **实测环境**：2026-09-14 ~ 2026-09-19，Windows 11 + Keil MDK 5.24a（编译器 V5.06u5）+
> ST-Link V2 克隆版，`<COM 口>` = COM7。SIM 是移动 NB-IoT 卡，模块固件 `BC28CNVBAR02A03`。
> 服务器是一台自己的 VPS。**本篇正文里所有服务器地址 / 域名 / token / 小区号一律写成占位符。**

**一句话结论：这一晚我动手三次，其中两次是错的 —— 而且错的两次，都比它们要治的病更伤人。**

1. **第一次**：查出一个真问题（socket 号会漏），于是加了一条**保险丝**。
   ★ 结果 **保险丝把板子打进了每 4 分钟一次的模块重启环**，而那次重启之后
   **一个号都没漏** —— 是**账本自己记歪了**（§3）。
2. **第二次**：抓到另一条真问题（模块重启之后 60 秒等不到网），于是把超时从 60 秒提到 300 秒。
   ★ **没有修好，只是让同一个死循环每圈慢了一点** —— 真正的错是
   **把"还没注上网"数成了"建连失败"**（§5）。
3. **第三次才对**：**把"没注网"和"连不上"彻底分开**，并且**换了一个不会记歪的判据**
   （§4、§5.3）。改完之后板子能自己安静地等网搜完，不再自己打断自己。

**而且这一晚最后落到一个更根本的问题上**：上面三次改的都是**"怎么救"**，
没有一次碰到**"为什么掉"**。因为**掉线那一刻板子在想什么，我们看不见。**
所以本篇真正的收口是 §7：**把不可观测的变成可观测**——

> 让板子把**当前小区号**放进每条上报里，看板上把「**换过几个小区**」和「**掉线缺口**」并排显示。
> **不是为了定位，是为了回答"那几次掉线前面，跟没跟着一次换小区"。**

---

## 0. 速查（熟了只看这一节）

```
① 先分清是"哪种连不上"——这一条能省掉本篇 80% 的弯路：

   注着网吗？          能开出 socket 吗？        结论
   ─────────────────────────────────────────────────────────────
   没注网              开不出                   ★ 不是故障，等它搜完
                                                别 NRB，别数 conn_fail
   注着网              开不出                   ★ 真的漏光了，NRB
   注着网              开得出、但连不上          ★ socket 层卡死，NRB
                                                但先看 ECL，见 ③

② 判据只会用这一个：AT+NSOCR 能不能开出号。
   ❌ 不要用 socket 账本的"余额"——那东西会自己记歪（§3）
   ❌ 不要用串口的 lost —— 那是"我没等到"，不是"对方没收到"（第 3 篇）
   ❌ 不要用 net[N] 判断重启 —— 那是个 10 秒一次的诊断计数器（§6.2）

③ 动手之前先看一眼 ECL / SNR：
   ECL=0 → 建连 3 秒     ECL=2 → 建连可以到 25 秒
   ★ ECL=2 的时候"建连超时"是常态，不是故障。这时候 NRB 是**有害的**。

④ 一次 NRB 的代价 = 重新搜网 2~8 分钟。**板子离线这么久。**
   所以"要不要 NRB"这个问题，永远先问一遍：它现在到底坏没坏？
```

**本篇改的三个文件**（第 5 篇那套工程上）：

| 文件 | 改了什么 |
|---|---|
| `bc28.h` / `bc28.c` | `BC28_TO_CLOSE 10000` + `BC28_CLOSE_TRY 3`（§2.2）；小区号进 `BC28_RADIO`（§7.1） |
| `Test/main.c` | 保险丝判据换成 `nosock_run`（§4）；`connect_failed()` 分流（§5.3） |
| `Test/stm32l1xx_it.c` | 无（它是第 6 篇的活儿） |

---

## 1. 症状：板子半夜老掉线

第 5 篇发布之后，看板上多了一栏「**掉线缺口**」（相邻两条上报间隔 > 5 分钟就记一次，第 5 篇 §10.1）。
它本来只是个显示项，结果上线第二天就把问题摆到了台面上：

| 时段 | 表现 |
|---|---|
| 白天 | 基本正常。30 秒一条，`[sock] ... balance=0` |
| **夜里 23:00 ~ 01:00** | **缺口一条接一条**。整段整段没有数据，一次几十分钟 |

而同一时间，串口上板子**一直在说话**：它没有死，它在**反复重试**。

**这就是最难查的一类故障**：板子自认为在工作（`AT` 有响应、`CSQ` 正常），
服务端一条都收不到。第 5 篇 §8.2 记过这个陷阱——

> **信号差能解释所有现象，所以它是最危险的答案。**

这一篇就是把那个陷阱**第二次**走进去、又退出来的全过程。第一次是第 5 篇的 socket 卡死，
这一次是它的"续集"：**同样的指纹，不同的病。**

---

## 2. 第一次动手：socket 账本 + 保险丝

### 2.1 先确认：真有一个"漏"

第一步是让**不可见的变可见**。`bc28.c` 里加了三个计数器：

```c
uint32_t BC28_SockOpenCnt(void);     /* 开 = AT+NSOCR 真拿到了号 */
uint32_t BC28_SockClosedCnt(void);   /* 关 = AT+NSOCL 真收到了 OK  */
int      BC28_SockBalance(void);     /* 上面两个之差 */
```

**注意"真收到 OK"这几个字**：回 `ERROR` 不算、超时不算。
主循环每轮打一行，很短：

```
  [sock] open=1 close=1 balance=0  nsocli=-1/0
```

很朴素的一个账本。**但它抓到了漏的那一下。**

### 2.2 漏的机理——一句话：**2 秒 vs 30 秒**

```
23:20:27  + DNS: <你的域名> -> <你的 VPS IP>     ← DNS 好的
          AT+NSOCO  ...                          ← 建连
23:20:57  ! NSOCO: no +NSOCO URC: timeout        ← 30 秒没等到 URC
23:20:57    raw<\n\n\n\n+NSOCLI: 1\n\n>          ← 模块正在拆某个旧连接
23:20:57  - connect failed (1 in a row)
23:21:00    [sock] open=2 close=1 balance=1      ← 开了一个，没关掉
```

三步，一步都不多余：

1. `AT+NSOCO` 超时 —— **TCP 建连没完成**，那个号还卡在重传里；
2. 代码调 `BC28_Close()`，但 `AT+NSOCL` **没回 OK** —— 模块不肯关一个它正忙着重传的号；
3. 于是**号没还回去**。**每失败一次漏一个，一共只有 6 个，漏光就全哑。**

**根因就一行宏。** `bc28.h` 里：

```c
#define BC28_TO_LONG    2000     /* ← BC28_Close() 用的是这个 */
#define BC28_TO_URC    30000     /* ← BC28_Connect() 用的是这个 */
```

> **`BC28_Close()` 给模块 2 秒，`BC28_Connect()` 给 30 秒。**
> 一个刚花 30 秒建连失败的 socket，模块正忙着在 TCP 层重传，
> **2 秒内根本回不上 `AT+NSOCL`** —— 这就是泄漏，一句话说完。

**铁证**（23:38:30 ~ 23:41:15，网络最差那 5 分钟，**每次 `NSOCO` 超时必跟着一次
`NSOCL` 失败，相关性 100%** —— 因为两者本来就是同一件事）：

```
[sock] open=1 close=1 balance=0     ← 网络好时 2 秒够，不漏
[sock] open=2 close=1 balance=1     ← 开始超时
[sock] open=5 close=1 balance=4     ← 5 轮开 5 个，只关掉 1 个
```

**修法**：

```c
#define BC28_TO_CLOSE  10000    /* 关一个 socket 给 10 秒 */
#define BC28_CLOSE_TRY 3        /* 失败隔 1 秒再试, 共 3 次 */
```

正常路径（模块秒答）一点都不多花；只有真卡住时才等。

**这一段是这一晚唯一一处"查对了并且改对了"的地方。** 后面两节全是错的。

### 2.3 加保险丝

光修掉根因不够 —— 模块里的号**不会因为 MCU 复位而放掉**（第 5 篇 §8.4），
所以历史遗留的泄漏得有人清。于是加了一条**保险丝**：

```c
/* 原来的写法: 6 个号里占了 4 个就主动 NRB */
if (bal >= 4 && bal > bal_prev)
{
    (void)hard_recover();       /* AT+NRB */
}
```

门槛取 **4 不是 1** —— 一次 NRB 要重新搜网好几分钟，代价不小；
1~2 的差额可能只是某次 `AT+NSOCL` 超时（模块其实关了）。

**推理看着完全成立。** 问题是：**这个推理的每一步都建在一个我没验的前提上。**

### 2.4 ★ 保险丝比它要防的病还伤人

上线当晚，板子被打进了**每 4 分钟一轮的 NRB 环**：

```
23:29:27  ! NSOCR failed ... 保险丝触发 → NRB
23:33:45  ! 又触发 → NRB
（每次搜网 1.5~2 分钟 —— 也就是说，板子几乎一半时间在重启）
```

**而那次重启之后，每一轮都是 `open=N close=N balance=0`。**
模块压根没漏。

> **保险丝比它要防的病还伤人。**
> 它防的是"漏光之后全哑"（几十分钟的停机），
> 它造成的是"**每 4 分钟主动停机 2 分钟**"（≈ 50% 的可用性）。
> 而且它把模块反复打断，**让模块永远搜不完一次网。**

**结论不是"门槛调到 5 就好了"** —— 是**这个判据本身是坏的**。下一节说为什么。

---

## 3. 反转：账本自己记歪了

### 3.1 铁证：socket 号从来不往上爬

保险丝反复触发之后，把日志里所有 `NSOCL` 失败的行拉出来对：

```
23:31:54  ! NSOCL=2 failed x3 (modem-ERROR)      ← raw 里刚有 +NSOCLI: 2
23:32:30  ! NSOCL=2 failed x3                    ← raw 里又一条 +NSOCLI: 2
23:33:06  ! NSOCL=2 failed x3                    ← +NSOCLI: 2, +NSOCLI: 1
23:33:42  ! NSOCL=1 failed x3                    ← +NSOCLI: 1
23:35:24  ! NSOCL=2 failed x3
```

**号一直是 1 和 2，从不往上爬。**

> **真在漏的话，`AT+NSOCR` 早该发出 3 / 4 / 5 / 6 号来了。**

**真实的循环长这样**：

```
① NSOCR 拿到 2 号
② NSOCO 等 30 秒超时              (网络差, TCP 建连没完成)
③ 网络自己把 2 号关了 → +NSOCLI: 2
④ 我们调 NSOCL=2 → modem-ERROR   (★ 号已经不存在了)
⑤ 账本 +1                          ← 记歪在这里
⑥ 回到 ①, 又拿到 2 号
```

**一个号都没漏，账本一路涨到 4。**

### 3.2 一个 `ERROR` 两种意思

根子在：**`AT+NSOCL` 回 `ERROR` 有两种含义，而账本只当成了一种。**

| | 含义 | 真相 |
|---|---|---|
| **(a)** | 关不掉、号还占着 | 真泄漏 |
| **(b)** | **这个号本来就已经没了** | **号早就还回去了** |

`+NSOCLI` 一到就说明是 (b)：网络刚通知"n 号我关了"，模块内部已经放掉，
我们紧接着去关一个不存在的号，当然回 `ERROR`。

**而 `bc28.c` 里 `BC28_Close()` 自己的注释早就写了这句话：**

```c
/* 已经关掉的 socket 再关一次, 模块回 ERROR, 无害 */
```

> **那句是对的。是账本把"无害"记成了"漏一个"。**

### 3.3 ★ 一条我觉得最该记住的教训

修完根因之后，当时记下一个很漂亮的数字：`[connect #1]` 到 `[connect #100]`
**连续 100 轮 `balance=0`**，服务端 `#125 UP=4238` 每轮 +30 秒。
当时把它当成了"修好了"的证据。

（**这个数字从来没进过正文** —— 账本是 09-17 才加的，第 5 篇 09-15 就发了。
这一篇是它第一次被写出来，**结果就是用来当反面教材的**。见下面。）

**但当晚 23:31 起，同样干净的代码又涨到 4。**
区别是：那 1 小时里**建连 3 秒就成、`+NSOCLI` 不出现**；
23:31 之后**建连要 25 秒、`+NSOCLI` 一条接一条**。

> **`balance=0` 只说明"这几轮网络好"，不是"泄漏治好了"。**

**所以：拿一个指标当"修好了"的判据之前，先问一句 ——
它在故障期会怎么表现？**

`balance` 在"网络好"和"真治好了"这两种世界里**表现完全一样**（都是 0）。
一个判据如果**在故障期和正常期长得一样**，它就没有诊断能力，
拿它当保险丝只会**在故障期疯狂误报** —— 这正是发生的事。

---

## 4. 换成真判据：**注着网却开不出号**

真正的"漏光指纹"只有一个，而且 `BC28_Open()` **早就把它分出来了**：

> **注着网，却连着几次一个号都开不出来**
> （`AT+NSOCR` 回 `ERROR` / 回 `OK` 但没带号 → `BC28_E_NOSOCK`）

主循环里改成：

```c
/* ★★ 保险丝 (2026-09-18 晚重写: 判据从**余额**换成**开不出号**) */
if (nosock_run >= 3)
{
    if (BC28_WaitNet(0) == 0)          /* ← 先问一句注网没有 */
    {
        printf("! NSOCR failed %d times in a row while ATTACHED --\r\n", nosock_run);
        printf("! sockets really are gone -- rebooting the module\r\n");
        nosock_run = 0;
        sock = -1;                     /* 模块要重启了, 手里这个号马上就不作数 */
        (void)hard_recover();
        connect_failed(3000);
        continue;
    }

    /* 没注网的时候 NSOCR 也回 ERROR —— 那**不是漏** */
    printf("! NSOCR failed %d times, but the module is NOT attached --\r\n", nosock_run);
    printf("! that is not a leak (NRB would eat the search), waiting\r\n");
    nosock_run = 0;
}
```

（`nosock_run` 只在 `sock == BC28_E_NOSOCK` 时累加，其它错误立刻清 0。）

**为什么这个判据稳** —— 它在两种世界里都对：

| 真实世界 | 判据的表现 | 该做什么 |
|---|---|---|
| **真漏了** | `NSOCR` 迟早失败 → `nosock_run` 到 3 | ✅ 该救的照救 |
| **账本记歪了** | `NSOCR` 一直成功 → `nosock_run` 永远是 0 | ✅ 该忍的忍住 |

而"余额"**只在后一种世界里必然出错** —— 也就是**在最需要它闭嘴的时候它最吵**。

**⚠ 那条 `BC28_WaitNet(0) == 0` 不能省。**
没注网的时候 `NSOCR` 也回 `ERROR`，不先问一句就又是同一个误报 ——
这跟 §5.3 那个"没注网就别 NRB"**是同一个坑踩第二次**。

`balance` 降级成**纯参考值**，仍然每轮打印（泄漏是"一轮漏一点"攒出来的，
只在出事时才打就看不到**它是从哪一轮开始斜的** —— 而那个拐点才是查根因的入口）。

---

## 5. ★ 第二个误判：**NRB 会吃掉正在进行的搜索**

### 5.1 60 秒的必然失败

上面两节修完，板子安静了几天。然后出现了一个新症状：

```
! rebooting BC28 (AT+NRB), this takes ~2 min (re-search) ...
（等 60 秒）
! rebooted but still not attached
```

`hard_recover()` 里当时写的是 `BC28_WaitNet(60000)` —— **只给 60 秒**。
而 NRB 之后模块**要从头搜网**，实测：

| NRB 时刻 | 注上时刻 | 花了 |
|---|---|---|
| 23:12:26 | 23:19:56 | **7.5 分钟** |
| 22:48:47 | 22:50:03 | 76 秒（这次算快的）|
| 23:06:35 | — | 300 秒等到头了还是没注上 |

**60 秒的等待必然失败。** 于是代码判"还是没注上" → 隔 3 秒再来 5 次 → **又 NRB**
→ **模块每次刚搜到一半就被打断。**

**那 22 小时里 NRB 发了几十次，一次都不可能成功。**

> **不是 NRB 治不了，是根本没给它时间。**

**这条也顺手推翻了一个当时记下的结论。** 那次 DNS 卡死的排查，当时的结论是
「**软重启解不了，只有断电解得了**」。现在看 —— **大概率是误判**：
用户换 USB 口让板子真掉了一次电，**那次之后没有再被打断**，于是搜完网自己就好了。
**断电之所以"管用"，是因为它之后那圈开机搜索没有被 NRB 打断。**

> **注意这个结论当时就只写在排查记录里，没有进过任何一篇正文** ——
> 而且**幸亏没进**。它当时是个**从两个现象倒推出来的解释**，
> 而不是"我掐掉 NRB 之后它好了"。**没验过的解释，不进正文**，这一条本篇也一样。

### 5.2 提到 300 秒并没有修好，只是让循环变慢了

第一版修法：把超时从 60 秒提到 300 秒（`BC28_TO_NET`）。**看着合理。**
实测：

```
23:41:15  NRB                                     ← 保险丝
23:46:49  ! rebooted but still not attached        ← 300 秒等到头
23:46:52  ! NSOCO: modem-ERROR                     ← 没注网, NSOCR 直接 ERROR
          ...(10 秒里连着 5 次, 每次瞬间失败)       ← ★ 一次 AT 都没真发出去
23:47:03  NRB                                      ← 又 NRB, 一圈 5 分钟
23:52:37  ! rebooted but still not attached
23:52:55  NRB
```

**循环没被打断，只是每圈从 1 分钟变成了 5 分钟。**

而重新搜网要 2~8 分钟 —— **它一次都搜不完。**

**关键错误是那一行注释指出的**：

> **把「模块还没注上网」数成了「建连失败」，5 次就 NRB。**

没注网的时候 `AT+NSOCR` 是**瞬间**回 `ERROR` 的（模块没附着，压根没有数据面），
所以"10 秒里连着 5 次失败"**不代表试了 5 次** —— 它代表**一次 AT 都没真发出去**。
**"5 次失败"这个计数器在没注网的世界里量的是空气。**

### 5.3 修法：把**没注网**和**连不上**彻底分开

```c
static void connect_failed(uint32_t wait_ms)
{
    conn_fail++;
    printf("- connect failed (%lu in a row), retry in %lu ms\r\n",
           (unsigned long)conn_fail, (unsigned long)wait_ms);

    if (conn_fail >= 5)
    {
        /* ★★ 只有"注上网了却连不上"才值得 NRB —— 那才是 socket 层卡死。
           压根没注网的时候 NRB 只会**把它刚搜到一半的网打断**。 */
        if (BC28_WaitNet(0) != 0)
        {
            printf("! 5 fails in a row, but the module is NOT attached --\r\n");
            printf("! waiting for the network instead of NRB (NRB would eat the search)\r\n");
            conn_fail = 0;                  /* 这不算"建连失败", 别记在它头上 */

            (void)BC28_WaitNet(600000);     /* 等它注上, 最长 10 分钟 */
            return;
        }

        (void)hard_recover();
    }

    BC28_Idle(wait_ms);
}
```

**那个 `conn_fail = 0` 是整段的核心。** 没注网**不算建连失败** ——
它连试都没试过，凭什么记在它头上。

10 分钟这个上限是按"实测最慢 7.5 分钟"定的，留了余量。

**实测有效**：00:00 左右板子在**没注网状态下安静等了约 5.5 分钟**，
自己注上，然后干净上报（`balance=0`，服务端 `#1~#4` 每轮 `UP` +30）。

**这是这一晚第一次，板子没有自己打断自己。**

---

## 6. 真相：网络差的时候，**超时是常态**

### 6.1 ECL 掉到底

改完之后把故障窗口的无线数据拉出来对照，才看清这一晚到底在什么环境里跑：

```
23:30:55  #28  RSRP=-69.7  SNR=+4.7  ECL=0    ← 好
23:34:58  #34  RSRP=-78.5  SNR=-8.7  ECL=2    ← 一路到底
```

**同一块板子，十几分钟内。** `ECL=2` 是最差的覆盖增强等级，`SNR` 变成负的。

而建连耗时跟着从 **3 秒涨到 25551 ms**（`[connect #6] 25551 ms`）：

```c
#define BC28_TO_URC    30000        /* 等 +NSOCO URC, 30 秒 */
```

> **30 秒的超时，实测用了 25.5 秒 —— 只剩 4.5 秒余量。**

**所以在这一晚的窗口里，"建连超时"是常态而不是故障。**
把它数成失败、再升级成 NRB，**等于每 4 分钟自己制造一次 2 分钟停机。**

**动手之前先看一眼 ECL / SNR** —— 这条写进 §0 速查了。

> ⚠ `BC28_TO_URC` **该往上提**（`ECL=2` 下 30 秒太紧）。**本篇没动** ——
> 一次只改一件事，而且当时更要紧的是上面那两个误判。记在 §9。

### 6.2 顺带：**别再数 `net[N]` 判断重启**

这一晚我自己也栽过一次。启动注网那段日志长这样：

```
20:30:06    net[1] CEREG stat=2
20:30:17    net[2] CEREG stat=2
...
20:34:25    net[25] CEREG stat=2
```

**这是 `BC28_WaitNet()` 内部每 10 秒打一行的诊断计数器**，不是重启计数器。

我当时的推理是：75 秒的采集里看到 `net[13]`，而更早一次采集里看到了 `net[14]`~`net[16]`
→ **计数器往回走了 → 板子重启了。**

**错的。** 那是**两次独立的 `BC28_WaitNet()` 调用**，各自从 `net[1]` 开始数。

> **要判板子有没有重启，看上报报文里的 `seq` / `UP` 有没有归零** ——
> 那两个才是真的"从头开始"。`net[N]` 只是当次等待的进度条。

---

## 7. 收口：把不可观测的变成可观测

前面六节改的都是「**怎么救**」。一次都没碰到「**为什么掉**」。

原因很直白：

> **掉线那一刻板子在想什么，我们看不见。**

看板上能看的只有：`RSRP` 从 -69 掉到 -100、出口 IP 整段换掉（实测
`117.136.x.x` → `39.144.x.x`）。**这两个都是"事后推论"，不是"现场证据"。**
头号嫌疑是**跳小区** —— 但之前只能猜。

### 7.1 让板子把**当前小区号**放进每条上报

**好消息是不用加任何硬件、也不用多发一条 AT 命令。**
小区号就在**已经取了的那条 `AT+NUESTATS`** 里：

```
AT+NUESTATS
  ...
  Cell ID: <28 位小区号>
  PCI:     <0~503>
  EARFCN:  ...
```

（顺手交叉验证过：`AT+CEREG=2` 之后 `AT+CEREG?` 回
`+CEREG:2,1,<TAC>,<CellID>,9`，那个十六进制的 `<CellID>` 和 `NUESTATS`
的十进制**完全对上**。`AT+COPS?` 回 `46000`，移动。）

所以 `BC28_RADIO` 加了两个字段，`BC28_GetRadio()` 里多解析两行：

```c
/* 当前小区 —— 就在上面这条 NUESTATS 里, **不额外发 AT 命令** */
(void)nstats_val(line, "Cell ID", &r->cell_id);
(void)nstats_val(line, "PCI",     &r->pci);
```

**两个都必须初始化为 `-1` 而不是 `0`** —— `0` 是个合法的小区号
（虽然实际几乎不会出现），拿 `0` 当"没读到"会让报文里分不清
"真读到 0"和"根本没读到"。

上报报文尾部追加：

```
#N,CSQ=..,RSRP=..,SNR=..,ECL=0,UP=..,T=..,CID=<28 位小区号>,PCI=<0~503>
```

**两个坑，都在"发得出去"这件事上：**

1. **值域检查同时也是字节预算检查。**
   `report_once()` 的报文拼进 `char body[128]`，而且**全是 `sprintf`，没有 `snprintf` 兜底**。
   最坏情况逐段数过 = 119 字节，含 `\r\n` 剩 9 字节。
   —— 靠的就是那道 `cell_id <= 268435455 && pci <= 503` 的值域护栏。
   没有它，`CID=-2147483647` 那种值能拼出 129 字节，**溢出 1 个字节**。
2. **只有值域合法才发。** 读不到就整段不发（看板上那一行显示"未知"），
   **不塞一个假的 0 出去** —— 这是第 5 篇 §7.2 那条"校验和通过 ≠ 数据有意义"的同一课。

> **⚠ 发布时的打码**：小区号 + PCI 合起来能反查到具体基站位置，
> **属于要打码的信息**（跟 IMSI / IMEI / 模块内网 IP 同一类）。
> 公开仓里一律写成 `<28 位小区号>` / `<0~503>`。

### 7.2 看板：**换过的小区**和**掉线缺口**并排

服务端侧：`sink.py` 给 `rx` 表**补两列**，看板 `payload()` 数"换小区"。

```python
def _db_migrate(c):
    """给**已经存在**的老库补列。
    ★ SQLite 没有 ADD COLUMN IF NOT EXISTS，只能先 PRAGMA 查一遍再补。
    ★ 外面套 try：**补列失败绝不能让 sink 起不来** —— 收数比这个功能重要得多。"""
```

看板上加了两行，**位置是故意选的**：

| 新增显示 | 放在哪 | 为什么放这 |
|---|---|---|
| 📡 当前小区（Cell ID · PCI） | 「最新一条」区 | 跟 RSRP/ECL 挨着，一眼看到"现在挂在哪" |
| 🔀 本区间换过小区 | 统计页，**紧挨着"掉线缺口"列表** | ★ **就是要对着看** |

> **每次断线前面，跟没跟着一次换小区。**

两个实现细节：

- **数"换小区"时必须跳过 NULL。** 老行（加列之前入库的）那一列是 NULL，
  不跳过的话"先有后无"会被数成一串换小区。
- **看板整页 JS 是嵌在 Python 字符串里的**，改完光看没用。
  上了个真解析器过一遍（`esprima`，装到 `/tmp` 不碰系统环境），
  20919 字符全过 —— 这道工序后来每次都跑。

**端到端验完**：报文 `...,CID=<28 位小区号>,PCI=<0~503>` →
数据库里 `cid` / `pci` 两列有值（老行仍是 NULL）→
`payload('1')` 回 `{"n_switch":0,"n_seen":1,...}`。

### 7.3 顺带说死：**定位这条路走不通**，两个死因

既然能拿到小区号了，很自然想问：**那能不能反查经纬度？**
**两条路都试过了，都是死的。** 写在这里免得以后重复试：

**死因一：模块自己不会算。**
这版固件（`SEBoot/V100R001C00SPC051`，`RADIO RF_010117_202209090109`）里
`AT+QLBS=?` / `AT+QLBS?` / `AT+QLBS` / `AT+QLBSCFG=?` / `AT+QGPS=?` / `AT+QENG=?` /
`AT+QNWINFO` **全部回 `ERROR`**。
能用的只有 `AT` / `CEREG` / `CSQ` / `NUESTATS` / `COPS?` / `CGATT?`。

**死因二：众包库必然没有这个小区。** 这条更值得记住：

> 这些库靠**手机 App 众包**上报「GPS + 小区号」，
> 而**手机根本不驻留 NB-IoT 小区** —— 没人路过上报它。
> **跟"是不是中国大陆"无关，是采集方式决定的。**

**实测对照**（免费、不用 key 的那家）：

| 查什么 | 结果 |
|---|---|
| 官方示例号（普通 4G 小区） | `errcode:0`，**精度 119 米、门牌级地址** |
| 我们的小区号 | `errcode:10001`（无查询结果）|
| 对照组 `lac=1&ci=1` | `errcode:10000`（参数错误）—— **证明 10001 真的是"库里没有"** |

**接口是好的、精度也是真的** —— 只是**这个小区不可能在里面**。

> **⚠ 做精度验证时的一个坑**：`coord=` 必须显式指定（`wgs84` / `gcj02` / `bd09`）。
> 拿 WGS84 直接贴到高德上会**凭空偏 300~600 米** ——
> **极易被误判成"这个库不准"。**

真要位置只剩运营商官方（中移物联网物联卡定位，误差 3~5 公里），
或者**手工填一个 `site.json`** —— 但那是"人填的"，不是上报的。

---

## 8. 失败判据（对号入座，别瞎试）

### 8.1 "连不上"到底该不该 NRB

| 注着网？ | 能开出 socket？ | 结论 | 动作 |
|---|---|---|---|
| ❌ 没注网 | ❌ 开不出 | **不是故障**，模块在搜网 | **等**（最长 10 分钟）。**别 NRB** |
| ✅ 注着网 | ❌ **连着几次开不出** | ★ 真的漏光了 | **NRB** |
| ✅ 注着网 | ✅ 开得出、但连不上 | socket 层卡死 | 先看 `ECL`（见下），再 NRB |
| ✅ 注着网 | ✅ 开得出、连得上 | 正常 | — |

**`ECL` 那一栏的判断**：

| 现象 | 大概率是什么 | 怎么办 |
|---|---|---|
| `ECL=2` / `SNR` 负值 / 建连 20 秒以上 | **无线环境差，超时是常态** | **别动**。`BC28_TO_URC` 放宽 |
| `ECL=0` / `SNR` 正常 / 建连 3 秒，却连不上 | 才轮到怀疑 socket 层 | 按上表 |

### 8.2 看日志的时候别被这几个东西骗

| 你看到 | 它其实是 | 该看什么 |
|---|---|---|
| `net[13]` 比之前小 | 两次独立的 `BC28_WaitNet()` 调用 | **报文里的 `seq` / `UP` 归零**才是重启 |
| `[sock] ... balance=4` | **账本可能记歪了** | 看 `AT+NSOCR` **还能不能开出号** |
| 串口 `lost=11` | "我没等到"，不是"对方没收到" | 服务端序号 + `seq − conn_n` + `UP` 三个数一起读 |
| `NSOCL=n failed (modem-ERROR)` | **号可能早就没了**，不是关不掉 | `raw<>` 里前面有没有 `+NSOCLI: n` |
| `nsocli=-1/0` 而 `raw` 里明明有 `+NSOCLI: 3` | **计数器坏了**，不是没收到 | 修计数器，别怀疑链路 |

> **最后那一行连着踩了三次，三次指纹一模一样、根因完全不同**：
> ① `BC28_Idle()` 每 20 毫秒扫一遍缓冲而**不清**，同一段 `+NSOCLI` 被反复计数（一小时涨到 719 万）；
> ② 修完变成**永远是 0** —— 位置票在"进块第一句"就被消费掉了，而那条 URC 是**一字节一字节**
> 收进来的，第一次扫到时缓冲里只有 `\n\n\n\n+NSOCLI`（**数字还没到**）；
> ③ `+NSOCLI: 1` **冒号后面有空格**（AT 标准格式），第一版解析忘了跳空格 —— 号一个都没记上。
>
> **两次教训**：
> **（a）诊断计数器要问"这段文字会被扫几次"；**
> **（b）把"见过"当成"处理过"是错的 —— 增量到达的数据里，"同一个位置"不代表同一份内容。**

---

## 9. 本篇没做 / 没定论（**照实说**）

| 事 | 状态 |
|---|---|
| **`BC28_TO_URC` 放宽** | ⚠️ **记下了，没动**。`ECL=2` 下实测用了 25551 ms / 30000，**只剩 4.5 秒余量**。一次只改一件事，改它要单独烧一次单独验 |
| **"等 10 分钟"那个上限** | ⚠️ **只在一个样本上验过**（实测 5.5 分钟自己注上）。**需要再攒几次** |
| **`connect_failed()` 的"5 次失败"** | ⚠️ 现在分了流，但"注着网却连不上"那一支仍会在坏窗口里 ~50/50 地触发 NRB。**坏无线下 NRB 是不是对的，没定论** |
| **socket 泄漏的触发条件** | ⚠️ **仍然没定论**（第 5 篇记过，本篇也没进展）。观察到的还是"网络差的时候"，**没有能复现的确定步骤** |
| **NSOCLI 计数器修好没有** | ⚠️ **没法证**。板子自己关 socket 时**根本不会有 `+NSOCLI`**（那条 URC 只在**对端先关**时才来），所以 `nsocli=0` 是对的、既不能证真也不能证伪。**它已经降级成纯诊断量**，保险丝用的是 `BC28_E_NOSOCK` |
| **换小区 = 掉线的原因吗** | ⚠️ **这正是 §7 要回答的问题，但数据还不够**。看板已经把两栏并排放好了，**需要跑几个晚上才攒得出相关性** |
| **经纬度** | ❌ **两个死因都写死了**（§7.3）。不再尝试 |
| **省电档（300 秒）下的掉线** | ❌ **本篇没测**。所有实测都在 30 秒档。间隔变长之后 `STALE_S` / `GAP_S` 要跟着配置走，那是第 6 篇的事 |
| **长连接下的掉线** | ❌ 没测。第 5 篇 §12 记着 `MODE_LONG=1` 只过得去 1 条就静默失效，**那个问题至今没修** |

---

## 10. 下一步

按我的推荐排序：

1. **把 §7 那个实验跑完** —— 让它跑几个晚上，再看「换过的小区」和「掉线缺口」对不对得上。
   这是**唯一一个已经把工具装好、只差时间**的事。因为 §7 的埋点已经烧进去了，
   **不需要再烧一次固件**（烧一次 = 板子离线 10~15 分钟，见下）。
2. **放宽 `BC28_TO_URC`** —— `ECL=2` 下 30 秒太紧是**有实测数字支撑**的。
   留到下一次不得不烧固件的时候**一起烧**，别为这一条单独烧一次。
3. **把「一次 NRB 的代价」做成看板上的一个数** ——
   现在每次 NRB 都在看板上留一条缺口，但**没人知道那是不是"值得的"**。
   服务端已经有 `conn` 表，可以顺手数出"每小时主动重启了几次"。

> **★ 补一条这一晚量出来的硬数字，它改变了"怎么排期"这件事：**
>
> **烧一次固件 = 板子离线 10~15 分钟。** 链路是
> ST-Link 复位 MCU → `BC28_Init()` 发 `AT+NRB` → 模块整机重启 → 重新搜网。
> 当晚量了两次：**9 分 45 秒**和 **12 分 20 秒**。
>
> **两条推论**：
> ① **别在模块正在搜网的时候烧** —— 那会把已经投入的几分钟搜索直接扔掉；
> ② **能攒到一起的改动就攒到一起烧**（上面第 2 条就是为这个）。

---

## 11. 本篇要跟哪几样东西一起发

| 一起发什么 | 为什么 | 在哪 |
|---|---|---|
| **① 本篇文档** | 就是这一份 | 仓库根目录 |
| **② `bc28.c` + `bc28.h`** | `BC28_TO_CLOSE`/`BC28_CLOSE_TRY`（§2.2）+ 小区号字段（§7.1）+ NSOCLI 计数器三次修正（§8.2） | [`06-BC28通信/`](06-BC28通信/) |
| **③ `main.c`** | 保险丝换判据（§4）+ `connect_failed()` 分流（§5.3）+ `,CID=,PCI=` 上报（§7.1） | [`07-多路采集/`](07-多路采集/) |
| **④ `main.c.diff`** | 让你看清相对第 5 篇**改了什么** | `07-多路采集/` |
| **⑤ 升级版 `sink.py`** | §7.2 那两列的迁移（`_db_migrate`） | `07-多路采集/` |
| **⑥ 看板 `nbiot_dash.py`** | 「📡 当前小区」+「🔀 换过的小区」（§7.2） | `07-多路采集/` |
| **⑦ 第 1 篇那套工具脚本** | 编译、烧录、看串口日志 | [`02-工具/`](02-工具/) |

> **⚠ 发布前必须做的事**（跟第 3 / 5 篇一样）：
> `main.c` 里的 `SERVER_HOST` / `SERVER_PORT` / `SINK_TOKEN` **一律换成 ASCII 占位符**；
> 正文里的服务器域名 / IP **一律占位符**；
> **小区号 `CID` 和 `PCI` 要打码**（§7.1 那条 —— 它能反查到具体基站位置）。
> 占位符要**看着就不像真的能用**，别让人拷了就跑。

---

## 附：本篇踩的坑，一句话一条

1. **★ `BC28_Close()` 给 2 秒、`BC28_Connect()` 给 30 秒** —— 一个刚花 30 秒建连失败的
   socket，2 秒内关不掉。**这就是 socket 泄漏，一行宏说完。**
2. **★ `AT+NSOCL` 回 `ERROR` 有两种意思** —— "关不掉"和"这个号本来就没了"。
   账本把它们记成了一种，于是"无害"变成了"漏一个"。
3. **★ 一个判据如果在故障期和正常期长得一样，它就没有诊断能力** ——
   `balance=0` 在"网络好"和"真治好了"两种世界里完全一样。拿它当保险丝只会疯狂误报。
4. **★ 保险丝可以比它要防的病更伤人** —— 防的是几十分钟停机，造成的是**每 4 分钟停机 2 分钟**。
5. **★ NRB 会吃掉正在进行的搜索** —— 搜网要 2~8 分钟，60 秒的等待必然失败，
   而失败之后又 NRB，模块**一次都搜不完**。
6. **★ 把"没注网"数成"建连失败"是致命的一步** —— 没注网时 `NSOCR` 是**瞬间**回 ERROR 的，
   "10 秒里 5 次失败"**代表一次 AT 都没真发出去**。
7. **★ 换个判据要挑"在两种世界里表现不同"的那个** ——
   "注着网却开不出号"是对的，"账本余额"是错的。
8. **`net[N]` 不是重启计数器** —— 它是 `BC28_WaitNet()` 每 10 秒一行的诊断。
   判重启看报文里的 `seq` / `UP` 归零。
9. **`ECL=2` 的时候建连超时是常态** —— 实测 25551 ms / 30000，只剩 4.5 秒余量。
   **先看 ECL 再决定要不要动手。**
10. **众包小区库必然没有 NB-IoT 小区** —— 手机不驻留 NB-IoT，没人上报。
    **跟是不是中国大陆无关。**
11. **`coord=` 不显式指定，会把责任推给"库不准"** ——
    拿 WGS84 贴高德凭空偏 300~600 米。
12. **`sprintf` 进定长缓冲要自己数值域** —— 这工程没有 `snprintf` 兜底，
    值域护栏同时就是字节预算护栏。
13. **诊断计数器要问"这段文字会被扫几次"**，
    而且**"见过"不等于"处理过"**（增量到达的数据里，同一位置不代表同一份内容）。
14. **★ 烧一次固件 = 板子离线 10~15 分钟**（实测 9 分 45 秒 / 12 分 20 秒）。
    **能攒的改动攒到一起烧；别在模块搜网的时候烧。**
