# STM32L151 + BC28 开发板 · 操作手册（第 5 篇：多路采集与云端看板）

> **系列目录 → [`README.md`](README.md)**

> ### 这是系列的第 5 篇
>
> **[第 1 篇](第1篇-搭好初始环境.md)** 跑通「编译 → 下载 → 串口打印」，
> **[第 2 篇](第2篇-BC28联网与AT指令实测.md)** 让 BC28 联上网、并用 PC 敲 AT 验证了收发，
> **[第 3 篇](第3篇-把数据发到自己的服务器.md)** 把「人敲指令」变成「固件自己发」，
> **[第 4 篇](第4篇-墨水屏点亮与显示实测.md)** 点亮了墨水屏。
> 本篇是系列的收口：**给板子加一路传感器、把数据送上云、再让它变成一张能点开看的网页。**
>
> | 篇 | 主题 | 状态 |
> |---|---|---|
> | [第 1 篇](第1篇-搭好初始环境.md) | 搭好初始环境（编译 / 下载 / 看日志） | ✅ 已发布 |
> | [第 2 篇](第2篇-BC28联网与AT指令实测.md) | BC28 联网 + AT 指令实测 | ✅ 已发布 |
> | [第 3 篇](第3篇-把数据发到自己的服务器.md) | 把数据发到自己的服务器 | ✅ 已发布 |
> | [第 4 篇](第4篇-墨水屏点亮与显示实测.md) | 墨水屏点亮 + 显示实测 | ✅ 已发布 |
> | **第 5 篇** | **多路采集 + 云端看板** | ✅ **本篇** |
>
> **本篇的写法**（跟前几篇一样，先划清楚）：
>
> - **不用厂家的 `DHT11.c`**，驱动整个自己写。这一篇里连**读时序的尺子**也是自己搭的（§3）。
> - **时序参数一律照抄手册或厂家**，改动过的地方一条条说明理由。
> - **我自己算出来和实测的数字分开写**；「推算」两个字会标出来。
> - **实验没做出来的结论，就写"没做出来"** —— §12 是完整的「没做 / 没定论」清单。
> - 没做、没验证过的，一律进 **§12**。
>
> **实测环境**：2026-09-14 ~ 2026-09-15，Windows 11 + Keil MDK 5.24a（编译器 V5.06u5）+
> ST-Link V2 克隆版，`<COM 口>` = COM7。SIM 是移动 NB-IoT 卡，模块固件 `BC28CNVBAR02A03`。
> 服务器是一台自己的 VPS。**本篇正文里所有服务器地址 / 域名 / token 一律写成占位符。**

> ### ⚠ 2026-09-15 修正（两条"安静地死掉"的路）
>
> 这一篇发出来之后，我自己的板子真的踩了一次 **§8.7 那个自动恢复没盖到的地方**：
> **开机注网失败时，`main()` 里走的是 `while (1)` 死等** —— 串口不出声、看板没数据、
> AT 也不回，**看起来跟板子烧了一模一样**，而且**断电重开还是这样**。查了一晚上。
> 同一晚还发现厂家模板的 `HardFault_Handler` 是 `while (1) {}`，触发即永久静默。
> 两处都已改（**§8.8**），`07-多路采集/` 里的 `main.c` 和 `stm32l1xx_it.c` 是修正后的版本。
> **如果你已经拿本篇的代码跑了，请把这两个文件换掉。**

**一句话结论**：**三件事都做成了，但顺序跟原计划不一样 —— 中间被一个硬件级的坑截了一次。**

1. **加一路温度**：DHT11 走 PB8（板上那个丝印 `+ B8 -` 的排针），自己写驱动，
   **实测 `start=20000`、余量 25µs / 19µs**（§6）。这颗传感器还带两个意外发现（§7）。
2. **一个把整轮排查带偏的坑**：**BC28 的 socket 层会自己卡死**，
   卡死之后 `AT+NSOCR` 回 `ERROR`、`AT+NSOCL` 关不掉，
   **而同一时刻 `CSQ=25` / `RSRP=-73`，无线好得很**。更阴的是
   **MCU 复位不会复位模块**，所以它跨烧录、跨复位键一直留着 ——
   表现就是「换了固件也一样坏」。**唯一解药是 `AT+NRB`**（§8）。
3. **云端看板**：把接收端从「只写纯文本日志」改成**同时落一份 SQLite**，
   再在已有的门户上加一个页面，点开就能看温度历史、运行时长、信号质量（§9、§10）。

**先说一句实话**：**经纬度这张页面上是空的，因为板子上没有 GPS。**
上报报文里只有 `#N,CSQ,RSRP,SNR,ECL,UP,T` —— 一个位置字段都没有。
这一条写在 §10.3，不藏着。

---

## 0. 三步速查（熟了只看这一节）

```
① 硬件：把那颗 3 针温湿度模块插到板上丝印 `+ B8 -` 的排针上
        ★ 插之前先认针 —— 两边丝印顺序**不一定一样**，认法见 §2
② 代码：在第 3 篇那份工程（bc28-socket）上，
        + 新增 07-多路采集/{dht11.c, dht11.h}  → 工程里的 HARDWARE/dht11/
        + 覆盖 07-多路采集/{main.c, bc28.c, bc28.h, stm32l1xx_it.c}
        + 按 07-多路采集/Project.uvproj.diff 改工程（2 处：IncludePath / 新增 dht11.c）
③ 先调传感器再联网：DHT11_SELFTEST 改成 1，重烧，抓 25 秒串口
        串口 → 10 组 "T=xx.xC raw=.. .." + 一组 start=/resp=/hi0<=/hi1>=
        调通后改回 0，重烧 —— 温湿度就并进每 30 秒那条上报里了
   服务端要多一份"能查历史"的数据，见 §9（sink.py 多落一份 SQLite）；
   要看网页，见 §10。
```

三个开关（都在 `main.c` 顶部）：

| 开关 | 作用 |
|---|---|
| `DHT11_SELFTEST` | **1 = 开机先连读 10 次温湿度**（调传感器专用）/ 0 = 关 |
| `MODE_LONG` | 1 = 长连接 / 0 = 每轮重连。**本篇实测跑的是 0**，原因见 §12 |
| `REPORT_MS` | 上报间隔，默认 30000 |

---

## 1. 本篇要做什么

第 3 篇结束时，板子已经能每 30 秒往自己的服务器发一行：

```
#12,CSQ=28,RSRP=-68.3,SNR=6.4,ECL=0,UP=360
```

那一行里**全是模块自己的状态** —— 信号多好、跑了多久。板子对**周围世界**一无所知。

本篇补上三块拼图，一块比一块靠上：

| 层 | 做什么 | 在哪一节 |
|---|---|---|
| **采集** | 加一路真实世界的量（温度），并写驱动 | §2 ~ §7 |
| **可靠** | 修掉一个「固件看着在跑、其实模块已经死了」的坑 | §8 |
| **呈现** | 把上报的数据变成**能点开看的网页**，而不是一行行日志 | §9 ~ §10 |

### 1.1 为什么是温度，为什么是 DHT11

板上那个排针丝印写着 `+ B8 -`，原理图里这个网络叫 **`STM32_PB8_BS18B20`** ——
**同一根线，DHT11 和 DS18B20 都能插，靠固件区分。** 手上正好有 DHT11 模块。

选它的另一个理由是：**它是这个系列里第一个"要跟时间较劲"的器件**。
前面几篇打交道的都是「发一条 AT、等一个回复」这种毫秒到秒级的宽松时序；
DHT11 的位宽只有 **26µs**，而且是**单总线**（一根线既发又收）。§3 讲的都是这件事。

> **⚠ 更正第 4 篇的一句话。** 第 4 篇 §2 写着「底板原理图里没有任何温湿度网络」，
> 由此判过「那个排针是死的」。**这句话是错的。** 真相是：原理图里确实没有叫
> `DHT11` 的网络，但**有一个走 PB8 的 DS18B20 网络**，丝印标 DHT11、原理图标 DS18B20。
> 当时 `while(DHT11_Init())` 死循环的真因是**没插传感器**，不是"没接"。
> 第 4 篇的**结论本身仍然成立**（那份例程还是不能直接烧），只是原因要换成这一条。
> —— 一处错误能一路带偏到「这块板子的墨水屏是坏的」，所以这里专门更正一下。

---

## 2. 硬件：接线（**插之前先认针**）

DHT11 模块是 3 针的：`VCC / DATA / GND`。**90% 的坑在这一步，而且插错会烧传感器。**

| | 左 | 中 | 右 |
|---|---|---|---|
| **板子排针丝印** | `+` | `B8` | `-` |
| **手上这颗模块丝印** | `+` | `out` | `-` |

**运气好的情况**：两边顺序一致（`+ / 数据 / -`），**3 芯壳直插就对**，不用接线。
核对方法就一句话：**模块的 `+` 要挨着板子的 `+`。**

**运气不好的情况**：另一块常见的模块丝印是 **`S / (未标记) / -`**，
中间脚**推断**是 VCC —— 那种**直插就是把 3.3V 打进数据脚**，可能烧传感器、也可能顶坏 PB8。

> **怎么在不拆模块的前提下认出哪根是 VCC？用万用表量电阻。**
> 3 针模块上的上拉电阻（4.7k~10k）**就接在 DATA 和 VCC 之间**。
> 所以：量 `S` 脚对另外两脚，**哪个读数在 4.7k~10k，哪个就是 VCC**，剩下那个是 GND。
> 读到的是开路（∞）就换个组合再量 —— 量不出 4.7k~10k 就先停下来，别上电。

还有两条：

- **板子这侧没有上拉电阻。** 原理图 PB8 网络上没有 —— 全靠**模块自带的那颗**。
  用裸传感器（不带小板的 4 脚 DHT11）就得自己补一颗 4.7k 到 3.3V。本板不需要。
- **杜邦线别超过 20cm。** 位宽只有 26µs，线长了上升沿变缓，读出来的 `hi0<=` / `hi1>=`
  余量会明显变小（§6 那行实测数就是用来发现这件事的）。

---

## 3. 核心设计决定：**测边沿**，而不是「延时后采样」

这是整个驱动唯一有技术难度的地方，也是本篇我觉得最值得讲的设计。

### 3.1 先说为什么不能"延时"

DHT11 的 40 位数据，**每一位的 0 和 1 只差在高电平宽度**：

```
bit = 0  →  50µs 低 + 26~28µs 高
bit = 1  →  50µs 低 +     70µs 高
```

判 0 还是判 1 的门槛取 **50µs**，两边余量各约 20µs。
**也就是说：需要一把"微秒级"的尺子，误差得远小于 20µs。**

**问题来了：本工程没有任何微秒延时可用。**

| 时基 | 归谁 | 能不能拿来做微秒延时 |
|---|---|---|
| TIM4 | `BC28_Millis()` | ❌ 1kHz 配置，改了就把 BC28 驱动的时间轴带偏 |
| SysTick | `main.c` 的 1ms 中断 | ❌ 拆了整机的节拍就没了 |
| 厂家 `delay.c` | — | ❌ **它会重写 `SysTick->LOAD/VAL/CTRL`**，直接破坏应用的中断时基 |

### 3.2 方案：TIM6 做 1MHz 自由跑计数器，把「宽度」算出来

开一个**不开中断**的 TIM6，预分频到 **1MHz** 让它自己一直跑，然后：

```
wait_edge(level, timeout, &t)   →  轮询 GPIOB->IDR 直到电平匹配，
                                   把 TIM6->CNT 的瞬时值写进 *t

高电平宽度 = (uint16_t)(t_fall - t_rise)      ← 16 位无符号相减
```

最后那个相减是关键：**(uint16_t) 的自然回绕正好处理了 65.5ms 的计数器溢出**，
不用写任何"跨没跨过溢出"的判断。

### 3.3 为什么是这条路，而不是另外两条

- **精度是晶振的，不是编译器的。** 32MHz 直接计数，边沿分辨率约 0.2~0.3µs，
  对着 20µs 的余量是 **70 倍安全系数**。
- **不受优化等级影响。** 本工程是 `Optim=1`（-O0），**任何 C 层延时循环的耗时都是
  "这一版代码生成"的产物，而不是 CPU 的性质** —— 改一行代码，延时全变。
  用计时器测就没有这个依赖。
- **不受中断延迟影响。** 最坏情况是一个约 5µs 的 USART1 中断，只会把**那一个**
  边沿的时刻推后 ≤5µs，而门槛余量有 20µs。
- **能自检。** 20ms 起始脉冲本身就是拿 TIM6 计时的 —— 打出来的 `start=`
  就是「这把尺子准不准」的读数。`start` 若不是 20000 上下，后面什么都不用查了。
- **顺带产出文章要的证据。** 它能打印**实测**的 `hi0<=` / `hi1>=`，
  也就是「门槛两边各有多少余量」。这个数是**这块板子当天量出来的**，不是抄手册的。

**否决的两条路**（理由写在 `dht11.c` 的文件头里）：

| 方案 | 为什么不用 |
|---|---|
| **DWT 的 `CYCCNT`** | 技术上可用（它是 CPU 周期计数器，精度更高），但 `CYCCNT` 在**调试电源域** —— 第 5 篇之后若加低功耗就会停摆，变成一个死循环。而且它跟调试器共用 DWT，**又没有第二个时钟能自检** |
| **`__NOP()` 标定循环** | -O0 下循环体是 `LDR/ADDS/STR/CMP/LDR/BLT`，耗时代码生成的产物，**标的是"这一版编译"而不是 CPU**。更致命的是它**只能延时、不能测量** —— 会把 20µs 的余量交给整个设计里最不可控的那一项 |

---

## 4. GPIO：**开漏输出 + 上拉，全程不切方向**

单总线要求同一根线既输出（发起始信号）又输入（读传感器）。常规写法是
「输出时配成推挽、读的时候切成输入」，**本驱动不这么做** —— 配一次开漏，之后只改数据位：

```c
GPIO_InitStructure.GPIO_Mode  = GPIO_Mode_OUT;
GPIO_InitStructure.GPIO_OType = GPIO_OType_OD;   /* 开漏是关键 */
GPIO_InitStructure.GPIO_PuPd  = GPIO_PuPd_UP;
GPIO_Init(GPIOB, &GPIO_InitStructure);
```

- **拉低**：`DHT11_PORT->BSRRH = DHT11_PIN;`
- **释放**：`DHT11_PORT->BSRRL = DHT11_PIN;`
- **读回**：`DHT11_PORT->IDR & DHT11_PIN`

**为什么开漏**：① 运行期**不需要切方向**（换向每次要 3~10µs，一帧 80 多次边沿来不及）；
② **不可能跟传感器打架** —— 推挽驱动高电平时传感器同时往下拉就是**直通短路**；
③ STM32 的输入施密特触发器除模拟模式外一直挂着，所以开漏时读 `IDR` 拿到的**就是线上真实电平**。

### 4.1 两条必须在代码里写死的纪律

1. **`DHT11_Init()` 里必须自己开 GPIOB 时钟。**
   全工程唯一一处 `RCC_AHBPeriph_GPIOB` 在 `usart.c` 的 `uart3_init()` 里，
   **而那个函数从来没被调用过**。不开时钟的话，对 PB8 的写是**静默失效**的 ——
   症状跟"传感器坏了"一模一样，但器件是好的、线是对的。这种坑最难查。
2. **边沿循环里直接读 `GPIOB->IDR`，不调 `GPIO_ReadInputDataBit()`。**
   那是**函数不是宏**，-O0 下每次调用有 1~2µs 的**变动**延迟 ——
   会是整个设计里最大的一项误差，而且它恰好落在 20µs 的余量上。

### 4.2 ★ STM32L1 的 CMSIS 坑：**没有 32 位的 `BSRR`**

按 F1/F4 的习惯写 `GPIOB->BSRR = ...`，在 **STM32L1 上直接编译不过**：

```
error:  #136: struct "<unnamed>" has no field "BSRR"
```

`stm32l1xx.h` 里 `GPIO_TypeDef` 是**两个 16 位寄存器**：

| 本工程用 | 作用 | 对应 F1/F4 的 |
|---|---|---|
| `BSRRL` | 低 16 位 = **置位** | `BSRR` 的低半 |
| `BSRRH` | 高 16 位 = **复位** | `BSRR` 的高半（`<<16` 那一半） |

**记法**：`BSRRL` = set、`BSRRH` = reset。名字跟直觉是反的，第一次写必踩。

---

## 5. 驱动 `dht11.c` / `dht11.h`

完全照 `bc28.h` / `bc28.c` 的规矩：`__DHT11_H` 卫哨、`DHT11_*` 前缀、
**`>=0` 成功 / `<0` 错误码**、`DHT11_Why(rc)` 翻字符串、C90（声明全在块首）、
`/* */` 注释、**字符串字面量一律 ASCII**（AC5 遇中文报 `warning #870-D`，本工程要求 0 Warning）。

### 5.1 错误码：**每个码对应一种物理原因**

手上只有一块板子的时候，错误码就是唯一的诊断手段。所以这里不写笼统的"失败"：

| 码 | 值 | 物理含义 |
|---|---|---|
| `DHT11_E_ARG` | -1 | 调用方传了 NULL |
| `DHT11_E_NORESP` | -2 | 起始信号发完了，线**一直没被拉低** → 传感器没接 / 没上电 / **插错脚** |
| `DHT11_E_STUCK` | -3 | 线**低下去再没起来** → 短路到地 / 上拉没接 / 传感器没供电 |
| `DHT11_E_TIMEOUT` | -4 | 40 位读到一半超时 → 时序被干扰，或供电不稳 |
| `DHT11_E_SUM` | -5 | 校验和不对 → 有位读错（偶尔一次是正常的，重读就好） |
| `DHT11_E_RANGE` | -6 | 校验**过了**但数值超量程 → 位错位，校验和偶然撞对 |

**响应信号要检查两次**（低 → 高 → 低）。只查"有没有变低"会把**短路到地**误判成
"传感器存在"—— 这正是 `E_NORESP` 和 `E_STUCK` 分成两个码的原因。

### 5.2 `DHT11_DATA`：数据 + **实测量**

```c
typedef struct
{
    /* ---- 数据(校验通过才有意义) ---- */
    uint8_t  humi, humi_dec, temp, temp_dec;
    uint8_t  raw[5];        /* 原样 5 个字节, 校验失败时靠它看错在哪 */
    uint8_t  sum_ok;

    /* ---- 实测量, 单位都是微秒 ---- */
    uint16_t start_us;      /* 我们拉低了多久(应当 ~20000) */
    uint16_t resp_low;      /* 传感器响应: 低电平宽度(手册 80) */
    uint16_t resp_high;     /* 传感器响应: 高电平宽度(手册 80) */
    uint16_t hi0_max;       /* 40 位里 **判为 0** 的最宽高电平(应当 <=28) */
    uint16_t hi1_min;       /* 40 位里 **判为 1** 的最窄高电平(应当 >=70) */
    uint16_t lo_max;        /* 位与位之间那个低电平的最大宽度(应当 ~50) */
    uint16_t frame_us;      /* 响应头 + 40 位一共花了多久(应当 ~4000) */
} DHT11_DATA;
```

**后半截那几个实测量不是调试残留，是唯一能证明"读对了"的东西。**
DHT11 的 0 和 1 只差在高电平宽度，把 `hi0_max` / `hi1_min` 打出来，就知道
门槛两边**各有多少余量** —— 余量小说明线太长或上拉太弱，**现在能读，换个环境就未必**。
接错线的时候也是靠这几个数一眼看出"时序根本不对"，而不是傻猜"传感器是不是坏了"。

### 5.3 读时序

```
拉低 20ms → 释放
  → 等响应低(80µs) → 等响应高(80µs) → 等低(第 0 位的低电平开头)
  → 循环 40 次 { 等上升沿记时刻、等下降沿记时刻、算宽度、>50µs 即 1 }
  → 校验和 (d0+d1+d2+d3)&0xFF == d4
  → 合理性检查（湿度 >100 或温度 >60 判 E_RANGE）
```

超时常数（微秒，每个都是"手册标称 × 余量"，注释里写明倍率）：

| 常数 | 值 | 依据 |
|---|---|---|
| `DHT11_T_START_US` | 20000 | 手册要求 ≥18ms |
| `DHT11_TO_RESP_US` | 100 | 手册 80µs |
| `DHT11_TO_HI_US` | 150 | 手册 70µs（bit 1 的高电平） |
| `DHT11_TO_FRAME_US` | 6000 | **整帧护栏** —— 保证阻塞时间有界，接错线也不会死循环 |
| `DHT11_T_HI_US` | 50 | 判 0/1 的门槛 |

---

## 6. 实测一：三段式验证

调硬件最容易犯的错是「一上来全接好，然后一次性看结果」——
出了问题分不清是**线**、是**驱动**、还是**整合**。所以这里分三段，
**每段只可能因为一个原因失败**。

### 6.1 阶段 0：先证明**错误路径会走**（不接传感器）

`DHT11_SELFTEST = 1`，PB8 上**什么都不接**，抓 25 秒串口。应当看到：

```
--- DHT11 self-test: 10 reads, 2 s apart ---
- [1/10] FAIL (-2): no response
          check: sensor powered? data wire on B8 (the middle pin)? module pin order right?
          start=20001 resp=0/0 hi0<=0 hi1>=0 lo<=0 frame=0
...
```

**这一步验了四件事**：① 起始脉冲真的发出来了；② **微秒尺子准**
（`start=` 不是 20000 上下就是 TIM6 分频配错了，后面都不必查）；
③ 超时路径会退出而不是死循环；④ 打印通了。

再把 PB8 短到 GND 重跑，应当得到 `(-3): line stuck` —— 验证第二条错误路径。

> **这一步必须在插传感器之前做。** 不然拿到一个失败结果，你永远不知道
> 是"没插"还是"插了但驱动有问题"。

### 6.2 阶段 1：接上传感器（模块仍不参与）

按 §2 的正确接法接好，同样 `-Seconds 25`，得到 10 组两行的成功读数。
**实测数字**（这颗传感器，2026-09-14）：

| 项 | **实测** | 手册标称 | 余量 |
|---|---|---|---|
| `start` | **20000** | ≥18000 | 尺子准到个位 |
| `resp` 低 / 高 | **82~84 / 85~87** | 80 / 80 | — |
| `hi0<=`（判 0 的最宽高电平） | **25** | 26~28 | **25µs** |
| `hi1>=`（判 1 的最窄高电平） | **69~70** | 70 | **19µs** |
| `lo<=`（位间低电平最大宽度） | **54** | 50 | — |
| `frame` | **3446~3448** | — | 一帧 3.4ms |

门槛取 **50µs** → **两侧余量约 25µs / 19µs**，跟 §3.3 说的"70 倍安全系数"是自洽的。

**最后一步是"吹气 / 捏住"**：温度要跟着动、松手要回落。
实测捏住从 **25 → 30.3 °C**、松手回落到 29.9，`UP` 单调下降 ——
**这才证明它是真读数，而不是一个看着像样的常数。**

### 6.3 阶段 2：整合

`DHT11_SELFTEST = 0`，重编重烧，抓 90 秒（3 个上报周期），确认报文尾部带上了 `,T=`：

```
[10] #10,CSQ=26,RSRP=-72.7,SNR=14.6,ECL=0,UP=454,T=26.8
```

**判据**：全程 `0 Error / 0 Warning`；`flash.log` 里有 `Application running ...`。

> **迭代技巧**：重烧是最慢的一步。10 次自检就是为了"**一次烧录换 20 个数据点**"。
> 要反复试优先**按复位键**重抓串口，而不是改一次烧一次。
> （烧录时 Keil 必须关着 —— ST-Link 会被占。）

---

## 7. 手上这颗传感器：两个意外发现

这两条是**数据自己说出来的**，不是查资料查到的。

### 7.1 它的温度是 **0.1 分辨率**，所以它**不是纯 DHT11**

真 DHT11 的温度分辨率是 1 摄氏度，**小数那两个字节恒为 0**。
但这颗给的 raw 是：

```
1A 09     →  26.9
1B 00     →  27.0     ← 中间那一步是**进位**(小数从 9 滚到 0, 整数 +1)
1B 01     →  27.1
```

协议一样、分辨率不一样 → 它是 **DHT12 或同协议的克隆片**。

**驱动不用改** —— `dht11.c` 本来就把 4 个字节都收全了，小数位照收。
上层 `dht_temp_str()` 也是按 `%d.%d` 打的，本来就能显示 26.9。

### 7.2 它的湿度通道**恒为 0**，但校验和是对的

raw 的前两个字节**每次都是 `00 00`**，而第 5 个字节仍然等于前 4 个之和。

**注意这里的推理**：40 位里错一位还**恰好撞对校验和**的概率是 1/256，
连中 10 次是不可能的。所以结论只能是一个：

> **不是读错位，是它真在发 0。**

房间里不可能有 0%RH（DHT11 的量程下限本身就是 20%），所以这一栏是坏的。
**用户拍板：不用管湿度，只要温度。** 上报里干脆不发 `,H=` ——
发出去就是往服务端灌假数据。以后换一颗好的把 `,H=` 加回来就行。

**这一条本身是个挺好的教训**：

> **校验和通过 ≠ 数据有意义。**
> `dht11.c` 里那道 `E_RANGE` 量程检查拦的是**超**量程，
> 拦不住这种"**看着完全合法、其实是 0**"的情况。

---

## 8. ★ 本篇最大的坑：模块的 socket 层会**自己卡死**

这一节是整篇最值钱的部分。它花了我们一整轮排查，而且**差点被写成"信号不好"结案**。

### 8.1 症状

板子串口显示它**一直在重试**，但服务端**一条都收不到**。
更迷惑的是：**模块对 `AT+NSOSD` 照样回 OK** —— 从固件视角看，一切都正常。

### 8.2 我们是怎么误判的（这一段照实写）

第一个直觉是「DHT11 干扰了时序」。**做了一个干净的 A/B 对照**：

```
git stash push -- main.c Project.uvproj     # 退回第 3 篇那版固件(不含 DHT11)
重编译 → 重烧 → 复现
```

**症状完全一样。** 再 `git stash pop` 干净还原。→ **传感器无辜，排除。**

第二个直觉是「服务端有问题」。在 VPS 本机直接起一条长连接，5 秒一条灌 12 条：
**12/12 全收**。→ **接收端无辜，排除。**

第三个直觉是「NB-IoT 信号差」。把板子上报里的信号历史提出来：

| 时间 | RSRP | SNR |
|---|---|---|
| 09-13 20~23 时 | -90.9 ~ -94.3 | 13.6 ~ 14.4 |
| 09-14 23 时（故障期间） | **-101.2** | **3.3** |
| 恢复后 | **-71.8** | CSQ=26 |

**看着像实锤了。** 但 —— **同一时刻**模块对 `AT+CSQ` 回的是 **25**、
`AT+NUESTATS` 回的是 **RSRP=-73**。**无线好得很。**

> **这里是最容易结案的地方。** "信号差"能解释一切现象，
> 而且当时的数据**确实**支持它 —— 如果不把 AT 侧的读数拉出来对照，
> 就会写成「NB-IoT 信号波动导致上报失败」，然后**永远修不好**。

### 8.3 诊断指纹（下次遇到直接对号入座）

真正的病根在串口里，要主动去看：

```
! NSOCR failed: modem-ERROR        ← 反复出现
```

于是让板子把 `AT+NSOCL` 也打出来：

```
AT+NSOCR=STREAM,6,0,1   ->  ERROR      ← 6 个 socket 全占着, 一个都分不出来
AT+NSOCL=<n>            ->  关不掉      ← 关不动
AT+CSQ                  ->  25         ← 同一时刻, 无线完全正常
AT+NUESTATS             ->  RSRP=-73
```

**两个关键指纹**：

1. **`AT+NSOCR` 返回的 socket 号逐次递增**（0 → 1 → 2 → 3 → 4），
   **而不是回到 0** —— 这是 socket 在泄漏。BC28 只有 **6 个** socket，漏满就全 ERROR。
2. **`AT+NSOCL` 关不掉。** 后果不只是"关不掉" ——
   **服务端那头永远收不到 FIN**，那条连接会一直**干挂到 600 秒超时**才被收走
   （这个 600 是接收端 `readline` 的超时，第 3 篇 §2.3 记过）。

### 8.4 ★ 为什么它这么难查：**MCU 复位不会复位模块**

这是整件事里最阴的一条：

> **MCU 和 BC28 是两套独立的复位。**
> 按板子的复位键、重新烧录、看门狗复位 —— **全都不会复位模块。**
> 模块上的 socket 会**一直泄漏下去**。

表现出来就是：**换了固件也一样坏**。你会以为是新代码的 bug，
于是去改代码、去 revert、去怀疑传感器 —— 而真正的病灶在一个你根本没碰过的地方。

### 8.5 解药：`AT+NRB`（整模块重启）

```c
void BC28_Reset(void)
{
    char tmp[32];

    rx_drop_all();

    /* 模块要是本来就在重启, 这句收不到 OK, 无所谓 —— 下面照样等 RDY */
    (void)at_cmd("AT+NRB", "OK", BC28_TO_LONG, NULL, 0);

    (void)wait_for("RDY", 30000, tmp, sizeof(tmp));
    BC28_Idle(1000);            /* 起来之后还得喘一口, 立刻发 AT 会丢 */
    rx_drop_all();
}
```

**`AT+NRB` 不回普通响应**：它先回一个 `OK`，然后模块**自己断电重来**，
起来时吐一条 **`RDY`**。所以要等两次、而且等得久（**实测约 10 秒**）。

**验证通过的标志**（这三个一起看才算数）：

```
socket=1                      ← 号回到低位了, 不再泄漏
+ server: AUTH OK
<- OK 333                     ← 服务端回了应答
```

**而且服务端这次是 2 秒内干净关闭的**：

```
23:56:10 AUTH   token ok
23:56:11 RX     #1,CSQ=26,RSRP=-71.8,SNR=3.5,ECL=0,UP=158,T=26.8
23:56:13 INFO   连接结束          ← 干净关闭, 说明模块 TCP 栈恢复健康
```

**副作用**：出口 IP 会变（重新注网拿新地址），实测 `117.136.x.x` → `39.144.x.x`。

### 8.6 「先试轻的，发现不够再上重的」

第一版写的是**开机无脑 `AT+NSOCL=0..5` 关一遍**。实测能释放一部分
（socket 号一度回到 0），**但模块卡死后 `AT+NSOCL` 本身就被拒，救不回来。**

**这个过程写进代码注释了** —— 因为"为什么不用更温和的办法"是后来的人一定会问的问题：

```c
/* 开机先把模块重启一次。
   一开始这里写的是"无脑 AT+NSOCL=0..5 关一遍", 实测**不够** ——
   模块卡死的时候 NSOCL 本身就被拒, 关不动。只有 NRB 能救。
   代价是每次开机多等十几秒重新注网, 换来的是"模块一定从干净状态开始"。 */
BC28_Reset();
```

### 8.7 自动恢复：连续失败 5 次就重启模块

光开机重启一次不够 —— 跑了几天之后还会再卡。所以 `main.c` 里加了一个计数器：

```c
static uint32_t conn_fail;    /* 连续建连失败了几次 —— 到阈值就重启模块 */

static void connect_failed(uint32_t wait_ms)
{
    conn_fail++;
    printf("- connect failed (%lu in a row), retry in %lu ms\r\n",
           (unsigned long)conn_fail, (unsigned long)wait_ms);

    if (conn_fail >= 5)
    {
        (void)hard_recover();       /* AT+NRB + 等注网(最多 60 秒) */
    }

    BC28_Idle(wait_ms);
}
```

`MODE_LONG` 的**两条失败路径都走这个出口**，成功时把 `conn_fail` 清 0 ——
免得同一套逻辑写两遍，也免得"偶尔抖一下"就把模块重启了（那要花 15 秒重新注网）。

> **⚠ 更正（写在后面的篇里）：这条"连续失败 5 次就 NRB"后来被证明
> 只在"注着网却连不上"的时候才对。**
>
> 如果模块**压根没注上网**，那"5 次失败"是**瞬间**发生的（没附着时 `AT+NSOCR`
> 直接回 `ERROR`）—— 它量的不是"试了 5 次"，而是"**一次 AT 都没真发出去**"。
> 这时候 NRB 只会**把模块搜到一半的网打断**，而重新搜网要 **2~8 分钟**，
> 于是变成"每 N 分钟自己制造一次几分钟停机"的死循环。
>
> **完整的机理、两个误判、以及"一次 NRB 到底多贵"，见第 7 篇 §5。**
>
> （另：本节 `hard_recover()` 注释里写的"等注网**最多 60 秒**"也是错的 ——
> 60 秒必然等不到，第 7 篇 §5.1 有实测的三个数字。）

### 8.8 补记（2026-09-15）：**启动那条路，当时漏了**

§8.7 那套自动恢复是给**主循环**写的 —— 管的是「跑着跑着掉线了」。
但**「一开始就没上来」是另一条路**，那条路上当时什么都没有。

`main()` 里 ② 注网原本长这样：

```c
rc = BC28_WaitNet(BC28_TO_NET);          /* BC28_TO_NET = 120000 */
if (rc < 0)
{
    printf("! network attach timeout: %s\r\n", BC28_Why(rc));
    while (1) { BC28_Idle(500); }        /* ← 只打一行, 然后永远静默 */
}
```

三个性质叠在一起，就成了一个**跟"板子坏了"完全无法区分**的故障：

| | |
|---|---|
| **① 注网期间本来就静默** | `BC28_WaitNet()` 轮询的 120 秒里**一句话都不打印**。所以"抓串口 0 字节"**不能**证明板子死了 —— 这点连排查的时候都很容易搞反 |
| **② 超时后永久静默** | 只打一行 `! network attach timeout`，然后不重试、不复位模块，**看板从此永远不会有数据** |
| **③ 断电重开救不了** | 开机 `BC28_Init()` 会发 `AT+NRB` 把模块重启，**每次上电都要重新注网** —— 等于每次开机重赌一次这 120 秒。所以"拔了重插"这种万能招数在这里**完全无效** |

于是症状是：**串口 0 字节 + 看板没数据 + AT 不回 + 断电重开还是一样**。
这四条凑在一起，任谁看都是"板子烧了"。

**改法**：超时就 `BC28_Reset()`（`AT+NRB`）再来一轮，每轮打印 `round N`，**永不静默退出**。

```c
for (;;)
{
    round++;
    printf("+ waiting for network (AT+CEREG?) ... round %lu\r\n", (unsigned long)round);
    rc = BC28_WaitNet(BC28_TO_NET);
    if (rc == 0) { break; }
    printf("! network attach timeout: %s\r\n", BC28_Why(rc));
    printf("! rebooting BC28 (AT+NRB) and trying again ...\r\n");
    BC28_Reset();
    BC28_Idle(1000);
}
```

**这里最值得记的一条**：§8.7 那套重试**主循环里早就有了**，我写的时候明明想到了"跑着跑着会掉"，
**就是没想到"一开始就没上来"**。同一个模块、同一类故障，**换了条路径就从脑子里漏掉了**。
排查的时候还因为「AT 不回」和「串口静默」把方向带到了硬件上 ——
**如果启动路径当时就打印 `round N`，这个坑第一分钟就现形了。**

### 8.8.1 顺带：`HardFault_Handler` 原来也是静默的

同一晚发现的第二处。厂家模板的 `stm32l1xx_it.c` 里：

```c
void HardFault_Handler(void)
{
  while (1) { }        /* 触发即永久静默 */
}
```

硬故障一旦发生，板子就**永远卡在这儿**，串口一个字没有 —— 形状跟上面那条一模一样：
"跑着跑着突然不出声、断电重开又好了、过一阵可能又犯"，而且**现场什么都不留下**。

改成先打现场再自愈：

```c
printf("\r\n!! HARDFAULT\r\n");
printf("   CFSR=%08lX  HFSR=%08lX  BFAR=%08lX\r\n", ...);
printf("   IBUSERR=%d PRECISERR=%d IMPRECISERR=%d UNDEFINSTR=%d"
       " UNALIGNED=%d DIVBYZERO=%d\r\n", ...);
/* 空转几百毫秒, 让 9600 波特的串口把话说完 —— 故意不调 BC28_Millis():
   故障状态下中断和时基都不保证还正常 */
NVIC_SystemReset();
```

有了 `CFSR` 就知道是**取指总线错误 / 精确数据总线错误 / 非对齐访问 / 除零**里的哪一种，不用再猜。

**注意 `NVIC_SystemReset()` 只重启 MCU，不会重启 BC28** ——
模块是应用起来之后 `BC28_Init()` 里那句 `AT+NRB` 才重启的（§8.4 那条在这里又出现了一次）。

> **照实说一句**：这两处改完烧回去之后板子一直正常，**所以"那次跑着跑着断掉"到底是不是 HardFault，
> 至今没有抓到实证**。加这段打印的意义就在于：**下次再犯，它会自己说出是哪一种故障**，
> 而不是又留下一晚上的空白。这属于**把不可查变成可查**，不等于已经查清。

### 8.9 一条更普适的教训

> **NB-IoT 应用里，「AT 能回 OK」不等于「模块是活的」。**

判模块健康要看的是：**socket 号有没有回到 0**、**服务端能不能干净关闭连接**。
这两个信号比任何 AT 响应都诚实。

---

## 9. 云端：从串口到网页

前四篇的"云端"其实只是**一行行日志**。第 3 篇的 `sink.py` 就是
「收到一行 → 追加到 `sink.log` → 回一个 `OK <n>`」，一共几十行。

**日志很好，但它回答不了"昨天下午温度是多少"。** 所以要再走一步。

### 9.1 为什么不直接查日志

`sink.log` 是**给人看的**（固定宽度、好 grep），但它是**纯文本**：

- 想画温度曲线得**每次重新解析整个文件**；
- 想按时间窗查（"近 24 小时"）得把所有行都读一遍；
- 解析逻辑散在查询的地方，改一处漏一处。

### 9.2 改法：**文本日志照旧，另落一份 SQLite**

原则是「**日志仍然是唯一真相**」—— DB 写失败只在 stderr 留一行，
**绝不影响收数、更不会断连接**：

```python
DB_FILE = os.environ.get("SINK_DB", "/opt/nbiot-sink/data/history.db")

# 板子报文形如:  #12,CSQ=28,RSRP=-69.1,SNR=13.6,ECL=0,UP=158,T=26.8
# 逐字段取值, **不按位置切** —— 老固件少发 T= 的那几条也能照样存下来(存成 NULL)。
RE_SEQ = re.compile(r"#(\d+)")
RE_KV  = re.compile(r"([A-Z]+)=(-?\d+(?:\.\d+)?)")
```

两张表：

| 表 | 一行是 | 用来算 |
|---|---|---|
| `rx` | 一条上报 | 温度历史、信号历史、**掉线缺口** |
| `conn` | **一次建连**（`AUTH` / `REJECT`） | 累计建连次数、被拒次数 |

**`conn` 表是这次才加的**，它让"板子到底连了几次"第一次变得可查 ——
第 3 篇 §7.1 那套「`seq − conn_n` 才是建连失败数」的判据，
**以前只能靠串口侧计数器**（而且随板子重启一起没了，第 3 篇记过这个教训），
现在服务端自己能数了。

三个实现细节：

- **`PRAGMA journal_mode=WAL`** —— 看板在读的同时还能继续写。
- **每次开一条新连接、写完就关**。一分钟才两三条，开销可以忽略，
  换来的是**没有跨线程共享的连接对象** —— `ThreadingTCPServer` 是多线程的，
  共享 `sqlite3` 连接默认不能跨线程用。
- **逐字段取值，不按位置切**。老固件少发 `,T=` 的那几条（第 3 篇期间发的）
  也能照样入库，存成 NULL 而已。

**顺带**：已有的 `sink.log` 可以**回填**进库，不用干等新数据 ——
回填脚本只插「早于库里最早那条」的记录，**幂等**，重复跑不会插重。
本篇实测回填了 **337 条**，上线就有历史曲线。

### 9.3 看板：一个页面，只读打开那个库

页面本身是一个自包含的 HTML（自己的 CSS + JS，canvas 画曲线，**不引任何 CDN**），
数据走一个 JSON 接口。它打开数据库的方式是**只读**的：

```python
c = sqlite3.connect("file:%s?mode=ro" % DB, uri=True, timeout=3)
```

**看板没有任何写权限** —— 采集那条路不受它影响，这比"小心别写错"可靠。

**挂进已有门户时有一个必须注意的点**：

> **路由要注册在 catch-all 反代路由之前。**
> 门户通常有一个「其余路径全部反代给上游」的兜底路由（形如 `/{path:path}`）。
> 按注册顺序匹配的话，**兜底写在前，新页面就永远轮不到**。

而且那一段用 `try` 包住 —— **看板崩了顶多这一页打不开，不能把整个门户带下水**：

```python
try:
    import nbiot_dash
except Exception as _exc:
    nbiot_dash = None
    _nb_err = "%s: %s" % (type(_exc).__name__, _exc)
```

---

## 10. 看板实测

### 10.1 页面上有什么

| 区块 | 内容 |
|---|---|
| 四个大数 | 室内温度 · **运行时长(uptime)** · 信号 CSQ · 在线 / 离线 |
| 最新一条 | 时间、温度、uptime、**本次上电于**、CSQ、RSRP/SNR、ECL、上报序号 `#N`、来源 IP、经纬度 |
| 🌡 温度历史 | 折线图，区间可切 `1h / 6h / 24h / 7天 / 全部` |
| 📊 统计 | 累计上报条数、累计建连次数、被拒次数、**掉线缺口**（>5 分钟一次，会列出断了多久） |
| 🕘 最近 50 条 | 原始记录表 |

两个衍生出来的数值得一提：

- **「本次上电于」** = `最后一条的时间 − UP`。`UP` 是板子自报的 uptime，
  所以这个倒推出来的时刻**不重启 MCU 就一直准**（模块重启不影响它）。
- **「掉线缺口」** = 相邻两条间隔 > 5 分钟（正常约 30 秒一条，含重连也就 1~2 分钟）。
  它把第 3 篇那套「别信 `lost`，要自己数序号」的判据**变成了页面上的一行数**。

### 10.2 验证

| 检查 | 结果 |
|---|---|
| 页面路由 | HTTP 200，页面 14.5 KB |
| JSON 接口 | `ok=true`，区间切换正常 |
| 门户卡片 | 已挂上，指向新页面 |
| **未登录访问** | **仍然给登录页** —— 没有绕过既有鉴权 |
| **串口 ↔ 数据库对照** | 串口发 `#10 UP=454` / `#11 UP=479`，库里**正好**新增两条，数字**逐字对上** |
| 编译 | `0 Error(s), 0 Warning(s)` |
| 烧录 | `Erase Done. Programming Done. Verify OK. Application running ...` |

最后那条"串口 ↔ 数据库对照"我觉得最重要 ——
**它证明信号从传感器一路走到网页，中间没有一处是假的。**

### 10.3 ⚠ 经纬度为什么是空的

**这块板子上没有 GPS，上报报文里也没有任何位置字段。**

板子发的就这七项：

```
#11,CSQ=26,RSRP=-72.9,SNR=9.3,ECL=0,UP=479,T=26.8
```

| 想要 | 有没有 | 说明 |
|---|---|---|
| 时间 | ✅ | 服务端收到时打的时间戳 |
| 室内温度 | ✅ | `T=` |
| uptime | ✅ | `UP=`（秒） |
| 经纬度 | ❌ | **硬件上没有** —— 板子上没有 GPS 模块 |
| 信号质量 | ✅ | `CSQ` / `RSRP` / `SNR` / `ECL` |
| 上报序号 | ✅ | `#N` |
| 连接次数 | ✅ | 服务端 `conn` 表自己数 |

**要让板子真的会报位置，得加一个 GPS 模块**（零焊接可选现成的串口 GPS，
接法跟 BC28 一样挂一路 UART，NMEA 语句解析比 DHT11 简单得多）。

**在加硬件之前**，页面上那一行可以**手工填**（放一个 `site.json` 就行）——
但那是"人填的"，**不是上报的**，页面上会照实标出来。

---

## 11. 失败判据（对号入座，别瞎试）

### 11.1 DHT11 侧

| 现象 | 大概率是什么 | 怎么办 |
|---|---|---|
| `(-2) no response`，`start=20000` | 传感器没接 / 没上电 / **插错脚** | 先认针（§2），再量 `S` 对另外两脚谁有 4.7k |
| `(-2) no response`，`start=` **不是 20000** | **TIM6 分频配错了** | 尺子坏了，后面都不必查 |
| `(-3) line stuck` | 短路到地 / 上拉没接 / 传感器没供电 | 量 PB8 对 GND 的电阻 |
| `(-5) checksum` 偶发 | 正常，重读 | 连续多次才要查线长 / 供电 |
| `(-6) range` | 位错位（校验和偶然撞对） | 看 `raw`，多半是线太长 |
| **`hi0<=` 接近 50 / `hi1>=` 接近 50** | **余量快没了** | 线缩短、上拉加强。**现在能读，换个环境就未必** |
| 温度是个漂亮的常数，捏它不动 | 读的不是传感器 | 检查是不是读到了残留缓冲 |

### 11.2 BC28 侧

| 现象 | 大概率是什么 | 怎么办 |
|---|---|---|
| `AT+NSOCR` 回 `ERROR`，**而 `CSQ` 正常** | **socket 泄漏光了**（§8） | `AT+NRB`。**别去查信号** |
| socket 号逐次递增不回 0 | **正在泄漏** | 同上，早发现早重启 |
| `AT+NSOCL` 关不掉 | 已经卡死了 | `AT+NRB`。**只有它能救** |
| `AT+NSOCR` 反复回 `ERROR` | socket 泄漏光了 | `AT+NRB`。**别去查信号** |
| 换了固件症状一模一样 | **模块状态跨复位留下来了** | `AT+NRB`。**MCU 复位不会复位模块** |
| 服务端连接干挂到 600 秒 | 收不到 FIN（`NSOCL` 被拒） | 同上 |
| **开机后前 2 分钟串口一个字都没有** | **正常**，不是故障 —— `BC28_WaitNet()` 轮询的 120 秒里就是不打印（§8.8） | **等满 120 秒**再判。别看到 0 字节就下结论 |
| **串口 0 字节 + 看板没数据 + AT 不回 + 断电重开还是一样** | **启动注网超时死等**（§8.8 那个 `while(1)`） | 换修正版 `main.c`。**这是固件问题，不是板子坏了** |
| **跑着跑着突然不出声，断电重开又好了** | 可能是 **HardFault 静默**（§8.8.1） | 换修正版 `stm32l1xx_it.c`；下次会打出 `CFSR` |

**一条最容易走的弯路**：把 §8 那种情况判成"NB-IoT 信号波动"。
**信号差能解释所有现象，所以它是最危险的答案。**
一定把 `AT+CSQ` / `AT+NUESTATS` 拉出来跟"连不上"的时间点对照 ——
**信号好而连不上，就是模块内部出事了。**

---

## 12. 本篇没做 / 没定论（**照实说**）

| 事 | 状态 |
|---|---|
| **长连接 (`MODE_LONG=1`)** | ❌ **本篇跑的是短连接（`MODE_LONG=0`）**。实测在**当前这条路径**上长连接**只过得去 1 条就静默失效** —— 服务端 `ss` 查不到那条连接、模块却继续对 `AT+NSOSD` 回 OK。换成第 3 篇那版固件（不含 DHT11）复现完全一样，**所以跟传感器无关**。同一晚短连接实测 **43/43 全中**。**长连接的自动重连要单独做一次改动** |
| **湿度** | ❌ 手上这颗**湿度通道恒为 0**（§7.2），用户拍板"不用管湿度，只要温度"。上报里**不发 `,H=`**。换传感器后加回来即可，驱动不用改 |
| **经纬度** | ❌ **硬件上没有 GPS**（§10.3）。页面那一行默认空着 |
| **`AT+NSOCL` 为什么会被拒** | ⚠️ **没查到**。只知道"卡死时它就是关不动"，**模块内部的真实状态没看到**（没有固件调试手段） |
| **socket 泄漏的触发条件** | ⚠️ **没定论**。观察到的是"跑一段时间之后"，但**没有能复现它的确定步骤** —— 所以现在的策略是"开机重启一次 + 连续失败 5 次再重启一次"，**是被动兜底，不是消除病根** |
| **长连接那条为什么只过 1 条** | ⚠️ **没查清**。已知"跟 DHT11 无关"（A/B 对照过），但**是模块、是运营商、还是路由，没定论** |
| **墨水屏联动** | ❌ 没做。第 4 篇算出「1 分钟刷 1 次能用 1 年 11 个月」，所以**把它当"每分钟更新的温度牌"在寿命上是可行的** —— 但**本篇没实测**这个组合 |
| **看板的长期稳定性** | ⚠️ 只验证了"数据能进去、页面能出来"，**没跑长时间**看 `history.db` 的增长速度（30 秒一条 ≈ 2880 条/天，按现在的行宽**推算**一年约十几 MB，**没实测**） |
| **`hi0<=` / `hi1>=` 随线长的变化** | ⚠️ 只测了当前这一根线。**没有做"多长开始出问题"的对照** |
| **`E_RANGE` 拦不住的"合法的 0"** | ⚠️ 知道它拦不住（§7.2），**没有加别的检查**。目前的处理是"干脆不发这一栏" |
| **2026-09-15 那次"跑着跑着断掉"的真因** | ⚠️ **没抓到实证**。板子连续正常上报 64 分钟（`UP=3843` 秒）后突然停，串口 0 字节、AT 不回。`UV4 -f` 烧录回 `Programming Done / Verify OK` 证明**芯片有电、SWD 通**，所以是固件/软件侧的问题；复位后恢复正常。**"是不是 HardFault"只是嫌疑，§8.8.1 那段打印就是为了下次能拿到证据** —— 在那之前不下结论 |
| **启动注网失败这条路是否真的发生过** | ⚠️ **没有当场抓到那行 `! network attach timeout`**。结论是"**代码里确实有这个死角 + 症状完全对得上**"，属于**修掉了一个确凿的隐患**，不等于"当场抓住了现行"（§8.8） |

---

## 13. 下一步

三条路，按我的推荐排序：

1. **长连接的自动重连**（把 `MODE_LONG=1` 那档救回来）。
   这是**唯一一个"已知坏着但没修"**的东西，而且第 3 篇的主线就是长连接。
   入手点：串口侧把"发出去但等不到应答"的次数打出来，
   跟服务端 `ss` 的实况对照，定位是"连接真断了"还是"模块以为还连着"。
2. **加 GPS**，把 §10.3 那个空填上。
   零焊接可行，NMEA 解析比 DHT11 时序简单得多 —— 而且**它会用到 §9 那套看板**，
   地图上一条轨迹比一行数字直观得多。
3. **墨水屏 + 温度**：第 4 篇已经把局刷和寿命那笔账算清楚了，
   现在数据也有了，**合起来就是这块板子（电子价签）的完整形态**。

---

## 14. 本篇要跟哪几样东西一起发

| 一起发什么 | 为什么 | 在哪 |
|---|---|---|
| **① 本篇文档** | 就是这一份 | 仓库根目录 |
| **② 驱动 `dht11.c` + `dht11.h`** | 本篇的核心，§3~§5 那一套 | [`07-多路采集/`](07-多路采集/) |
| **③ `main.c`** | 加了 `,T=` 上报、自检块、`conn_fail`/`hard_recover()`；**含 §8.8 的启动重试修正** | `07-多路采集/` |
| **④ `bc28.c` + `bc28.h`** | §8 那个 `BC28_Reset()` | `07-多路采集/` |
| **④′ `stm32l1xx_it.c`** | **§8.8.1 的 `HardFault_Handler` 修正**（厂家模板是 `while(1){}`） | `07-多路采集/` |
| **⑤ `main.c.diff`** | 让你看清相对第 3 篇**改了什么**（含 §8.8 的修正） | `07-多路采集/` |
| **⑤′ `stm32l1xx_it.c.diff`** | 相对**厂家模板**改了什么（只有 `HardFault_Handler` 一处） | `07-多路采集/` |
| **⑥ `Project.uvproj.diff`** | 工程改动**只有两处**（IncludePath / 新增 `dht11.c`） | `07-多路采集/` |
| **⑦ 接收端 `sink.py`（升级版）** | §9 那套「文本 + SQLite 双落」 | `07-多路采集/` |
| **⑧ 看板 `nbiot_dash.py` + 回填脚本** | §10 那个页面（**自包含，不引 CDN**） | `07-多路采集/` |
| **⑨ 第 1 篇那套工具脚本** | 编译、烧录、看串口日志（§0 第 ③ 步要用） | [`02-工具/`](02-工具/) |
| **⑩ 厂家资料包** | 骨架工程、`DHT11.c` 对照件、墨水屏数据手册都在里面 | **只能跟卖家/作者要**（3.4 G 传不上仓库） |

> **⚠ 发布前必须做的事**（跟第 3 篇一样）：`main.c` 里的
> `SERVER_HOST` / `SERVER_PORT` / `SINK_TOKEN` **一律换成 ASCII 占位符**。
> 占位符要**看着就不像真的能用**，别让人拷了就跑。

---

## 附：本篇踩的坑，一句话一条

1. **DHT11 模块的引脚顺序不一定是 `VCC/DATA/GND`** —— 量 `S` 对另外两脚谁有 4.7k 再插。
2. **`DHT11_Init()` 必须自己开 GPIOB 时钟** —— 那个时钟在**从没被调用过的** `uart3_init()` 里，不开就是静默失效。
3. **STM32L1 没有 32 位 `BSRR`**，只有 `BSRRL`（置位）/ `BSRRH`（复位）。
4. **边沿循环里别调函数** —— `GPIO_ReadInputDataBit()` 是函数，-O0 下 1~2µs，正好吃掉 20µs 余量。
5. **校验和通过 ≠ 数据有意义** —— 湿度恒 0 但校验和对，是它真在发 0。
6. **DHT11 温度整数部分的最高位是符号位** —— 按无符号打，-5°C 会变成 251°C。
7. **★ MCU 复位不会复位 BC28** —— 模块状态跨烧录、跨复位键一直留着，"换了固件也一样坏"。
8. **★ `AT` 能回 OK ≠ 模块是活的** —— socket 泄漏光时，`CSQ` / `RSRP` 完全正常，只有 `AT+NSOCR` 回 ERROR。
9. **★ 信号差能解释一切现象，所以它是最危险的答案** —— 一定要拿 AT 侧读数跟故障时间点对照。
10. **门户加页面，路由要注册在 catch-all 之前** —— 按注册顺序匹配，兜底写在前就永远轮不到。
11. **★ 启动注网失败时 `while(1)` 死等 = 故障和"板子坏了"一模一样** —— 串口不出声、看板没数据、AT 也不回，**断电重开还是这样**（§8.8）。
12. **★ 厂家模板的 `HardFault_Handler` 是 `while (1) {}`** —— 触发即永久静默，正好解释"跑着跑着突然不出声"（§8.8.1）。
13. **同一个故障换条路径就会漏掉** —— §8.7 的重试主循环里早就有，**启动那条路就是没想到**（§8.8）。
14. **"抓串口 0 字节"不能证明板子死了** —— `BC28_WaitNet()` 那 120 秒本来就不打印（§8.8）。
